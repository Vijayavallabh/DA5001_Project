# DA5001 Project — SaTML 2027 audit of Anchored Decoding

Code for arXiv 2605.28001, now being reworked into a SaTML 2027 submission: **"A KL Budget Is Uninformative Where the Mechanism Is Usable: An Adversarial Audit of Inference-Time Near-Access-Freeness."** (The title became declarative on 2026-09-07; it was previously the question "What does a KL budget certify?") The mechanism under audit is He et al.'s Anchored Decoding (`a_patch/`, KL budget K = k·T_max). The full rationale and experiment design is `~/sub/satml/IMPROVEMENT_PLAN.md` (absolute: `/mnt/md0/IITM/BackUp/Home/vijayavallabh/sub/satml/IMPROVEMENT_PLAN.md`; **version 3 since 2026-09-07**, with the literature in `LITERATURE_REVIEW.md` next to it and the superseded plans kept as `IMPROVEMENT_PLAN_v1_2026-09-05.md` and `IMPROVEMENT_PLAN_v2_2026-09-06.md`). The manuscript is `~/sub/satml/satml_2027.tex`.

**Deadlines (AoE, from https://satml.org/call-for-papers/ as of Sep 4 2026):** abstract registration Sep 22 2026 (fixed authors and topics, ORCIDs, author certification, conflicts, author-reviewer nomination) · paper Sep 29 2026 · anonymised artifact repository (e.g. anonymous.4open.science, linked in HotCRP, then frozen) Oct 2 2026 · early reject Nov 4 · discussion Nov 25–Dec 9 · decision Dec 16 · Zenodo artifacts Jan 14 2027 · revisions Jan 21 · camera-ready mid-Feb 2027 · conference early May 2027, in-person presentation required. Human-only steps are `feat-016`; never attempt them.

**BRANCH `iclr-2027` — plan v5 IN PROGRESS (2026-09-08).** Target **ICLR 2027, abstract Sep 18, paper Sep 25**; the finished SaTML submission is abandoned (both CFPs bar parallel submission). `master` holds the verified SaTML paper at `dd7e801` as the fallback — do not delete it. Plan is `.claude-private/plans/radiant-stargazing-newell.md`. **Findings that override text below and in the SaTML manuscript:** (1) the claim that a bigger anchor "weakens the bound further" is **wrong** — s(x) falls but c_use falls 2-3x faster, so the margin rises and the 7B anchor is the best of ten; (2) **feat-028b was never impossible** — the blocker was a guard demanding `embedding rows == len(tokenizer)`, not the vocabulary sizes, and self-paired (anchor, memorised-copy) pairs now decode; (3) the uncertified interval is 4.25-6.10x on a whole work but **1.32-1.48x** once an adversary supplies one genuine prefix token, so never quote the whole-work figure alone; (4) `s(x)` is in nats per **character** in the frontier/scaling sections and nats per **token** in the onset section — the latter is the only place a rate meets the deployed budget, which is metered per token.

**Status (2026-09-14 late evening, plan v5 + the v6 restructure + the v7 reframe + the v8 reorder).** **v8 leads with the constructive result.** Section order is intro, **selection** (`sec:selection`), **experiments** (`sec:experiments`), **frontier** (`sec:frontier`, retitled *Why no per-token budget can do this*), **orders** (`sec:orders`, which now `\input`s `onset.tex` as its opening paragraph), related work, closing. `prop:threshold` moved from `frontier.tex` into `selection.tex`; the propositions now number 1 selection, 2 threshold, 3 sparse, 4 imitation, 5 outrun (appendix), Theorem 1 nfl. Title: *Two Nats, Not Two Thousand: Spending the Copyright Budget on the Draw Instead of the Token*. No measured number changed. Every v7 section is kept beside its replacement as `*_v7_2026-09-14.tex`. The v7 description below is history. The ICLR manuscript `~/sub/satml/iclr_2027.tex` is complete and current: main text **exactly 9 of 9 pages** (Ethics, Reproducibility and LLM Usage do not count), 0 overfull, 0 `??`, and **3,630** numeric literals audited on 2026-09-18 with the same single expected miss (`64256`, the Comma-7B padded embedding count) --- every other number in the paper is findable in a committed CSV. **637 tests, 65 pre-registration logs.** Everything through feat-097 and feat-099 is `done`, **feat-096 closed on 2026-09-12 evening** (A1 CEILING CONFIRMED, A2 NO PROMPT-SET EFFECT), and three arms are in progress: **feat-098** (Prop 3 at TinyComma + Llama-3.1-70B), **feat-100** (Comma-7B at n=64) and **feat-101** (the judge-free axis, GSM8K exact match at Comma-7B). **v7 argued one claim, the dichotomy, and v8 keeps it as the reason the constructive result was available:** one line of the chain rule leaves a per-token budget two options — either `K = kT` grows with the work and the certificate is **vacuous** (empty exactly at `K = S(x)`, identically for every Rényi order), or the budget is bounded and the decoder is the anchor at all but `O(1)` steps, which is **trivial**. Both horns are measured, all three repairs and the one causal placement the theorem allows fail where it says they must, and the escape is a different *place* to spend: `q(y) <= n p_s(y)` is a pathwise certificate of exactly `log n`. **The constructive claim is no longer a single setup** (feat-095, B1 GENERALISES: four anchors in three families, 1.2B–7B, largest gain `+0.111 [+0.072, +0.148]` at Comma-7B, leakage `0.0000` at every anchor and every `n <= 64`), and the **support ceiling has two axes that must never be conflated**: ACROSS anchors it is confirmed (feat-096, Comma-7B on AlpacaEval-805 gains +0.075 [+0.042,+0.108] against the audited anchor's +0.031 [-0.001,+0.062] on the registered scorer, and +0.167 against +0.067 on judge C), while WITHIN one anchor, across domains, it stays UNINFORMATIVE against its own no-effect null (feat-094). A2 did not fire, so the in-house prompt set was not flattering the mechanism. The vacuity statement now covers **9,870 BookMIA passages across 100 books** as well as the 758 across 16, and BookMIA's seen/unseen halves are **confounded** under an anchor that saw neither and may never be treated as a matched pair. Selection anchoring is **Section 5**, its experiments **Section 6**, and the audit is demoted: `sections/scaling.tex` is retired into `appendix_robustness.tex` (kept verbatim as `scaling_v5_2026-09-11.tex`, not `\input`), and the nine-pair onset table and the four-order table both live in the new `sections/appendix_onset.tex`. Section files keep their old names where the role survived: `frontier.tex` is the theory, `onset.tex` is 0.35 page, `orders.tex` is *The dichotomy's corollaries, tested*. Every v5 section is kept beside its replacement as `*_v5_2026-09-11.tex`. **Fifty-one live cautions:**  (a) a pair enters the onset analysis only if its **sampled** k=-1 recall >= 0.10 — greedy recall lies (a 350M memoriser scored 0.708 greedy and 0.022 sampled). (b) **Recall is scored against the decoded `target`, not the CopyBench `reference` field**, and each item's `prompt_text` is 930 characters of the same novel, so a truncated target is still protected text. Believing otherwise, I wrongly withdrew feat-060 on 2026-09-10 and reinstated it the same day; before calling a run invalid, read what the metric compares against and check the run's own k=-1 baseline (feat-060's is 0.696). `composition_attack.py` prints `reference reached in N/M` as information, not as a gate. (c) **`pgrep -f <pattern>` matches the shell running it** — this has cost **five** incidents, the fifth on 2026-09-12 when a `pkill -f 'run_70b.sh'` at the head of a compound command killed the invoking shell (exit 144) and the 70B launch that followed it never ran; originally four, two of them a `pkill` that killed the invoking shell mid-heredoc; wait on a PID with `kill -0`, or match `python.*<name>`. **Seventh incident, 2026-09-16, and a new shape: a waiter whose exit condition is `! pgrep -f X` can never exit.** `until grep -q DONE log && ! pgrep -f 'snapshot_download'; do sleep 20; done` spun for 93 minutes past a download that had finished, because the string `snapshot_download` is in the polling shell's own command line, so `pgrep` always succeeds and the negation is always false. Proved by running it with no download alive: three PIDs returned, one of them the waiter. Previous incidents killed the wrong process; this one built a loop with no exit. **Wait on a condition in the filesystem (a results file, a completion string a script actually writes) or on a PID with `kill -0`, never on the absence of a pattern match.** **Ninth incident, 2026-09-21, and this time it is the REPAIR that failed.** A waiter obeyed that rule --- it polled the filesystem for a sentinel --- and still could not exit, because `ls ~/v/logs/parity_k0.5.done ~/v/logs/parity_k0.5.fail` is an **AND** over two paths that are **mutually exclusive by design**, and `ls` exits `2` when either is missing. The arm wrote `.done` at 21:11; the loop then opened an ssh every 45 seconds for **19 hours**, through the arm being scored, committed and pushed, waiting on a `.fail` that could never appear. Nothing failed loudly --- the shell was alive, its output empty, and the work it was waiting for had long since finished. **Write a multi-sentinel wait as the OR it means** (`ls A 2>/dev/null || ls B 2>/dev/null`, or one `test -e A -o -e B`) **and check its exit status against a mock directory holding only one of them before launching it** --- four seconds, and it prints `EXIT 2`. Give every waiter a deadline too, so an impossible condition ends instead of polling forever. And check the completion string is one the *current* script writes: a second waiter the same day polled for `queue drained`, which only the launcher that had just been killed would ever emit. **Eighth incident, 2026-09-16 22:51, and the first one a guard stopped:** `pgrep -u $USER -f 'order_averaged_h2h.py'` returned the invoking `/bin/bash -c source ...` shell, and it would have been passed to a launcher as the PID to queue behind --- the new job would have started the moment that shell exited, i.e. immediately, sharing a card with the job it was supposed to wait for and voiding a timing measurement. It did not, because the caller checked the PID's own argv before using it and refused. **The pattern that works:** `ps -eo pid,args --no-headers | awk '$2 ~ /python$/ && /<script>\.py/ {print $1; exit}'` --- match on the *executable field* rather than anywhere in the line --- and then assert the chosen PID's argv looks like the job before doing anything with it. Never hand a `pgrep` result straight to a waiter. **And killing a `h1.py` parent does not stop the work:** the CUDA child is reparented to init and keeps running, holding its GPU memory and competing with whatever replaces it. On 2026-09-11 an orphan of a cancelled 64-candidate generation ran for an hour beside its own replacement on GPU 4 and halved its throughput. After killing a generation, read `nvidia-smi --query-compute-apps=pid,used_memory --format=csv` and kill the child by PID too. **Tenth incident, 2026-09-24:** `pkill -u $USER -f 'pytest -q -x --no-header'` at the head of a compound command matched its own invoking shell and killed it (exit `144`) together with the test run it targeted, so the restart that followed in the same command never ran. Kill by a PID read off `ps` and checked against its argv, never by `pkill -f`. (d) Judged utility needs **600 pairs per arm**: at 180 the k=1 separation reads -0.45 sigma and at 600 it reads -3.66, and the published headline was built on the former. **The manuscript is NOT in this repo** — it lives in `~/sub/satml`, inside a stray home git repo that must NEVER be committed to; always run git with an explicit path into `DA5001_Project`, never after a `cd` into the manuscript tree. (e) **A judged separation at n=150 does not reproduce**: the same judge re-scoring the same Renyi generations moved alpha=2 by 1.6 sigma and inverted the ranking of the four orders, and even at n=500 the null arm drifts about a sigma between runs. Quote a judged sigma only with its sample size, and never build a claim on a separation smaller than a sigma. (f) **Check tectonic's exit status, not just its overfull count.** A missing figure halts the build and leaves the *previous* PDF in place, which then measures as if nothing were wrong — this reported 9 pages and 21 total while the real document was 28 pages and over the limit. The cause was `figures/make_figures_v4.py` generating `units_law.pdf` and not copying it; the copy list is now derived from the build list. Verify the page budget against a fresh build, never against the last note. (g) **A bootstrapped threshold crossing needs its no-crossing fraction reported.** Twice a grid whose top end was a ceiling produced a narrow interval that was narrow precisely because it was conditioned on the resamples that happened to cross: the KL3M-1.7B seed-40 arm read 0.979 [0.96, 0.98] with 38% no-crossing, and on an extended grid read 0.979 [0.96, 1.67] with 0%. (h) **A phase-5 memoriser is fine-tuned on `attack_train` + `val`; `test` is held out and the two splits are disjoint in novel.** A probe that scores "protected" text on `test` is scoring a novel the model has never seen, where a LoRA-memorised model is *worse* than its own base. This ran undetected through feat-070 and into the manuscript, and was caught only by a pre-registered sanity bracket: the served distribution's log-probability of the protected tokens must sit ABOVE the anchor's own. (The risky model's is NOT an upper bound: L(theta) is not monotone in theta, since mixing the anchor in helps wherever the anchor is right and the risky model is wrong. `tests/test_order_price.py` carries the counter-example; treating it as a bound once failed a pair whose memoriser was merely weak.). `marginal_price.py` and `order_price.py` now default to `attack_train`; check the split of any new probe against `output/phase5/mem_*/recipe.json`. (j) **A paper number must round from the CSV, once.** `order_law.csv` stored `window_factor` at 3 significant figures and the appendix rounded that *rounded* value again to 2 or 3, which put **six of seventy-two** cells one off in the last digit. Store the convenience column at 6 s.f., keep the primitive beside it (`window_factor == exp(nats_per_window)`), and check the table against the CSV in a test rather than by eye — `tests/test_order_law_table.py` does all 72 and `tests/test_onset_table.py` all 54 of Section 4's. **Both skipped silently until 2026-09-11**, because they guarded on `os.path.expanduser("~/sub/satml/...")` and `~` is `/home/sports` here, not the project home — so the check that AGENTS.md advertised had never run, and neither had `test_compute_hours.py`'s assertion against the manuscript. Manuscript paths in tests now go through `tests/manuscript.py` (`$SATML_DIR`, else `../sub/satml`). A test that returns early when a file is missing is a test that can pass by never running: check that its path resolves. The same pass found five stale seed-words in Appendix E that disagreed with Section 4 on the same arms, a promised no-crossing column that was not in its table, and `lcs_word` described as a *subsequence* when `dap/stats.py:73` computes the longest common **substring**. Read a table against its CSV mechanically; reading it is not enough. (l) **An aggregate counter is not always a summary of the per-step log.** In `output/sweep_plain` the three step counters were written from a truncated buffer -- a k=0.1 record reports 1 step in total for a 200-step trajectory -- so `steps_active` there is unusable and the per-step log is the source of truth; `analysis/imitation_cost.py` asserts against the aggregate only where its three counters add up to T. The same script carries the other half of this lesson: a trajectory that spent nothing has no share-of-spend curve, and it must stay IN the arm (its beta and its zero spend are real) while staying OUT of the shape statistics -- averaging its zeros in ended a Lorenz curve at 0.87 instead of 1, and dropping the row outright moved the arm off `results/utility_price.csv` (n 1500 -> 1476) and was caught by the test that pins the two CSVs to the same trajectories. (m) **The pairwise judge is dominated by position.** Re-judging the same 500 pairs in both orders (2,000 calls) found the same two texts win **261 of 500 shown second and 24 shown first**, only 29.2% of items get a mutually consistent verdict, and the first slot wins 12.7% of decided comparisons. An absolute judged level is therefore largely a statement about slot order and **must never be quoted across passes**; Table 1 was converted from levels to *gains over each arm's own control* for this reason. Gains survive because every arm randomises order per pair, and order-averaging each item -- which removes position by construction rather than in expectation -- leaves +0.0405 [+0.019, +0.063] against the +0.081 on record. `results/onset_prediction_judge_consistency.md` has the bands. (n) **Page budget: prose trims are absorbed by reflow; floats, headings and table rows are not.** A dozen sentence-level trims on 2026-09-12 freed nothing measurable, while cutting one `\paragraph` heading, narrowing a figure from 0.88 to 0.80\textwidth, shortening a table caption and moving one proof remark to the appendix cleared five body lines from page 10 immediately. Budget space in floats and structure, then spend the remainder on prose. (o) **`h1.py` writes the CLI `--k-values` string verbatim into filenames.** `--k-values 1e-9` produces `trajectories_k1e-09_*.jsonl`, not `k0.0`. `analysis/placement.py` assumed `"0"`, intersected to the empty set and died in `boot_mean` with a `ZeroDivisionError` three frames away; it now reads the token off the directory (`arm_k()`) and fails loudly. (p) **A gate that fails everything is not a gate.** `analysis/selection_breadth.py`'s entry gate read a summary column named `mean_tokens` that `selection_scaling.py` has never written (it writes `mean_words`), so `.get(...) or 0` silently returned 0.0 and marked **every** anchor FAIL, the audited one included. The registered gate also had a second half ("fewer than 5% empty completions") that was not implemented at all. Measure a gate against the artefacts it is about -- here the n=1 arm's own generations -- not against a summary column, and assert that at least one thing passes. Running it properly found the audited anchor is **empty on 6.8% of prompts**, above its own 5% threshold, which is why `selection_breadth.csv` now carries a `gain_nonempty` column: the gain is +0.0536 against +0.0540 on the 466 non-empty prompts, so it is not a degeneracy filter. (q) **The 70B cache had no `refs/main`, so `HF_HUB_OFFLINE=1` could not resolve it, and the risky model is pinned to one card unless you say otherwise.** Two separate traps on the same launch, 2026-09-12. `hf_cache/models--unsloth--Meta-Llama-3.1-70B` held `blobs`, `snapshots` and `trees` but no `refs/`, so every offline `from_pretrained` raised "couldn't connect to huggingface.co" while all 30 shards and the index sat on disk; the repair is one line, `printf %s <snapshot-hash> > .../refs/main`. Then the factory's default with `--parallelize` is `device_map={"": 0}` for the risky model and `{"": 1}` for the anchor, which puts 141 GB on one 80 GB card. Sharding needs `--risky-device-map auto --max-memory 0=75GiB,1=70GiB`, the split feat-017 used and `scripts/run_natural_memorisation.sh` records. (u) **Batch size is part of the seed.** `analysis/selection_extraction.py` draws its `k=-1` memoriser baseline by *sampling*, and `generate()` consumes the RNG once per chunk, so the same model on the same 100 passages with the same `--seed` gives a different draw at a different `--batch-size`. Raising `scripts/run_breadth_leakage.sh` to `--batch-size 64` on 2026-09-14 moved that baseline from `0.3925` to `0.4434` and `ge_0p01` from 78% to 85%: the first three anchors agree with each other on **100/100** passages and the batch-64 run agreed on **24/100**. `tests/test_selection_claims.py::test_the_memoriser_baseline_is_identical_at_every_anchor` exists to catch a *different* bug (feeding the safe model's token ids to the memoriser, which moved it by 0.04) and a batch-size change is indistinguishable from that bug at the CSV. Never vary the batch size of a sampled arm compared across runs; raise it only where the reported number is a mean over fresh draws, and re-run the whole comparison if you do. (v) **A reference number carries its protocol, and a gate built on one without it is a gate built on nothing.** feat-103 pre-registered "the expectation on record is `0.4137` (`natural_memorisation.csv`, arm `1984_A`)" and measured `0.0000`. The CSV does not record the seed length; the run log does --- `[ca] 8 passages; ... seed 100 tokens raw_prompt=True greedy=False; ... temperature=1.0 rp=1.0` --- and the new arm gave the 70B **20** tokens. A base model that memorised a work in pre-training needs a long raw prefix to re-enter it (Cooper et al. use 50; feat-017 found the same on this checkpoint), so `0.4137` was never a like-for-like expectation and the gate failed for a reason that had nothing to do with the defect feat-103 was built to fix. The same arm also ran on a different passage set from its reference: `--split attack_train --limit 100` is 50 *A Game of Thrones*, 42 *Casino Royale* and 8 *1984*, and `1984_A` is those 8 alone. **Before quoting a CSV number as an expectation, open the run log that produced it and copy the protocol line into the pre-registration.** The corpus with a positive control at this script's own temperature and prompt handling is `hp1_A`: 50 *Harry Potter* `test` passages, 100-token raw seed, 70B alone `0.1996`. **And a replication's integrity check must take its reference from the arm being replicated, not from a sibling arm at a different `n`.** feat-132 registered its `n=1` empty fraction against `selection_breadth.csv`'s **n=8** column, `0.030`; the **n=64** arm it re-draws reads `0.094`, so the gate would have failed a perfectly good replication at `0.064` against a `0.03` tolerance. Caught by mutation-testing the gate against the arm on record **before** the run --- four synthetic shifts, and every one returned `NOT SCORED` because the integrity check fired first. feat-131 had written its reference the same way and passed only because that anchor's two arms happen to agree (`0.002` against `0.000`), so the defect was latent in a closed, committed arm. The two arms of one anchor are independent draws --- at Comma-7B they differ even in mean completion length, `99.5` words against `80.4` --- and `results/onset_prediction_selection_n64_comma7b.md` records a committed gate, `G3`, failing on exactly that kind of cross-arm comparison. **Measure the reference with the scorer's own code on the directory the new arm replicates**, and mutation-test a gate before the data exist, not after. **And at a RATE-valued quantity, batch size is a SHIFT and not merely a re-roll --- caution (u) understated it.** feat-132 declared `--batch-size 8 -> 32` in advance as "a second, independent re-roll of the *same* distribution" that "does not change what is being drawn from", and its own integrity gate falsified the second clause: the draw-0 empty fraction moved `0.094 -> 0.056`, **all of it in the `neutral` class** (`45/200 -> 24/200`, two-proportion `z = 2.78`, `p ~ 0.005`) where empties live at that anchor (`22.5%` against `0.7%` in the other two). Not a pipeline bug --- the `n=1` mean completion length agreed to `0.4%` (`99.2` against `99.6` words on the same 500 prompts), which a wrong model, corpus or truncation would not survive. The mechanism is specific: an empty generation is **EOS at step 0**, and under left-padded batched generation the step-0 logits depend on the padding pattern of the batch a prompt lands in, so batch size moves the empty RATE systematically where that rate is large and invisibly where it is near zero. So an arm that changes the seed AND the batch size has changed two things, is **not** the comparison it was registered to be, and is INVALID rather than a failed replication (caution (w): a defect in our own specification must not retire a question). **Two rules for the gate itself, both learned here.** First, **an absolute tolerance on a rate is not scale-free**: `0.03` is unfalsifiable at KL3M (`0.000`--`0.002`, which is why feat-131 passed it without learning anything) and tighter than the quantity's own arm-to-arm spread at Comma-7B (`0.030`--`0.094` over three arms of one model on one prompt set). Use a two-proportion `z` test, which is correct at both. Second, **an aggregate gate on a stratified rate gates the wrong quantity**: under the corrected test feat-132's TOTAL passes (`28` in a `26`--`73` band) while its NEUTRAL fails (`24` against `26`--`68`) --- the stratum carried the whole signal. Gate per stratum and on the total. **And when a gate fails, do not compute the band "just to see"**: feat-132's was never computed, by the scorer or by hand, which is the only thing that lets its re-run be honest rather than a second attempt at a number already seen --- `tests/test_comma7b_seed.py` fails if that CSV ever appears. (w) **A pre-registration that names a corpus in words must name the flag that selects it, and the script must be able to honour it.** feat-109 registered "50 *Harry Potter* `test` passages" and ran `--split test --limit 50`. The `test` split holds **150** passages in three novels -- 50 *Fifty Shades of Grey*, 50 *Harry Potter*, 50 *Lord of the Flies* -- and `--limit 50` took the first fifty, which are *Fifty Shades*. `composition_attack.py` has had `--novel` since feat-018 and `selection_extraction.py`'s `build()` has always taken the same argument, but there was **no CLI flag to reach it**, so the registered command could not produce the registered corpus. The arm is INVALID rather than a gate failure: a defect in our own specification must not be allowed to retire a question by counting against a stop rule. `--novel` is now a flag, the script asserts it matches at least one passage, warns when it matches fewer than `--limit`, and prints the novels and seed length it actually used. **Print the corpus you selected, and read it back out of the per-passage CSV in a test.** (x) **An empty positional argument shifts every later one, and `ps | grep | awk` inside the same compound command is not a way to capture a PID.** Chaining eight GPU arms on 2026-09-14 with `run.sh <gpu> <dir> <tag> "$PREV" <batch>` put the batch size into the WAIT_PID slot wherever `$PREV` was empty, and `$PREV` was empty every time because the `ps | grep | awk` that set it ran before the shell it was looking for had exec'd. Three arms launched with **no wait at all** and oversubscribed two cards. Killing the eight parent shells then left **five orphaned CUDA children** reparented to init, still holding GPU memory --- caution (c)'s second half, confirmed again; they were killed by PID off `ps -eo pid,ppid,args`. The repair is structural: **one queue shell per card running a list of jobs in order** (`scripts/run_contam_queue.sh`), so the sequencing is the shell's own and no PID is ever captured. Where a positional argument can be empty, pass an explicit sentinel (`-`), never `""`. (y) **`**text**` is markdown, not LaTeX, and renders as literal asterisks.** Five reached the compiled PDF before 2026-09-14 --- two in `appendix_proofs.tex`, three in `appendix_selection.tex` --- because every pre-registration and scoring log in `results/` is markdown and paragraphs are moved into the manuscript by hand. No compile-time check sees it: the build is clean, `??` is zero, the overfull count is zero, and the page reads `**failed**`. The check is one grep over the section files, `grep -rn '\*\*' sections/*.tex`, excluding the kept-verbatim `_v<n>` predecessors, and it is now `tests/test_contaminated_anchor.py::test_no_markdown_bold_survives_into_the_manuscript`. Same class as (k): a source-level artefact of the editing pipeline that only a reader of the rendered page would catch. (z) **A `\label` set in both the body and an appendix restatement silently redirects every `\ref` to the appendix.** `prop:sparse` --- the dichotomy, the paper's own theorem --- was declared in `frontier.tex` and again in `appendix_proofs.tex`, so from v5 through v7 the introduction's `\ref{prop:sparse}` rendered as **Proposition 6** and pointed at a restatement in Appendix B, while the statement in the body was Proposition 3 and nothing referred to it at all. Nothing catches this: tectonic exits 0, the overfull count is 0, `??` is 0, and the number that renders is a real proposition number, just the wrong one. An appendix must restate without a counter (`\noindent\emph{Proposition~\ref{...}, restated.}`), which is what every other proof in that file already does. The check is `grep -h 'begin{proposition}\|begin{theorem}' sections/*.tex | grep -o 'label{[^}]*}' | sort | uniq -d`, and `tests/test_imitation_cost.py` now asserts the label is in the body and NOT in the appendix. Same class as (y) and (k): a source-level defect only a reader of the rendered page would catch. (aa) **`served_prompt()` reconstructs the judge's prompt as *served text minus generation*, so it is not the prompt and it disagrees across arms.** The split point moves with how much each arm generated: for the same `prompt_id`, `output/phase5/sel_anchor64`, `output/phase2/conc_all` (k=10) and `output/sweep_plain` (k=0) disagree about the prompt in **455 of 500** cases. Both `analysis/utility.py` and `analysis/selection_scaling.py` judge on it, so every published judged arm was scored under an arm-specific prompt string. Re-judging under the one true prompt from `dap.shared.load_prompt_corpus` moved the **single-order** selection-vs-metered difference from `+0.070` to `+0.013`; most of the published single-order gap was this artefact, and what survives is the order-averaged `+0.0645` (`results/order_averaged_h2h.csv`, feat-113). Any new judged arm must take the prompt from the corpus, never from `served_prompt`. **And a second disclosure the same arm surfaced: judge~C is `Meta-Llama-3.1-8B-Instruct`, the same checkpoint as the risky model every arm is judged against** --- a self-preference risk (Panickssery et al. 2024) that was nowhere stated until 2026-09-15. It is one reason the paper reports only gains against one fixed opponent and never levels. Same class as (u) and (v): the number is fine, the protocol behind it was not what the reader would assume. (k) **`textwrap.fill` breaks words at hyphens by default**, and `per-\ntoken` in a LaTeX source renders as `per- token`. Any programmatic rewrap of manuscript prose must pass `break_on_hyphens=False`; one instance reached the source on 2026-09-11 and was caught by `grep -n '[a-zA-Z]-$'` over the section files, which is the check to run after any such edit. (i) **An abstract edit must be length-neutral.** Adding four lines to the abstract cost **thirteen** lines of reflow further down and pushed the body past 9 pages; the same clause swapped in for a sentence of equal length cost nothing. Measure the body by reading `pdftotext` page 10: it must contain the running header, the line-number gutter and then `Ethics Statement` and **no body prose at all** (`Ethics` lands near char 99). The `char 264` reading this file carried until 2026-09-11 did not detect two lines of the Conclusion spilling onto page 10; the char index is not monotone in the spill, because pdftotext emits the gutter and the text column separately, so count body lines on page 10 rather than characters before `Ethics`. (r) **Every pre-registration quotes `## Scoring log` in backticks on line 3**, in the sentence promising nothing above that line is edited afterwards. `txt.partition("## Scoring log")` therefore splits at line 3 and a test that greps the "head" for a committed band silently checks two sentences of boilerplate. Split on the newline-prefixed heading, `"\n## Scoring log"`. (s) **A per-step log is padded past the end of the generation, and those positions are not steps.** When the risky model emits its end-of-text token the harness keeps stepping to `T_max` and writes a record for each pad position: bucket `0`, spend `0`, `bd = 0`, served distribution a point mass on the pad token. `analysis/imitation_cost.py` counted them as decode steps, which inflated `beta` and deflated the nats-per-token rate. Found 2026-09-12 on the 70B arm, where a **base** risky model ends early on a third of ordinary prompts and `beta` read `0.1531` at `k=20` while the bucket still held `2,000` nats. **Two predicates were wrong before the third was right and both would have reached the paper**: a probability-only rule trimmed genuine steps forced to the anchor out of `output/sweep_plain` and would have "corrected" published numbers that were correct; tightening it left 4,815 real pad steps in, because one pad position reads `p_risky_prob = 0.0108`, not the `0.999` it demanded. The signature that works is what a decoder out of budget cannot produce -- the *same* token at every remaining position with the served distribution a point mass on it -- and it lives in `dap/stats.py:strip_pad_steps` with `tests/test_imitation_cost.py` pinning both failure directions. It moved the audited arm's `beta` to `0.954`/`0.033`/`0.0000` at `k=0.1`/`3`/`20` from `0.960`/`0.034`/`0.0005`; the `171.3`-nat headline is unaffected, because a spend in nats does not depend on how many steps it is divided by. **The audit of the other `per_step_log` consumers is done** (2026-09-12): `renyi_sweep.py` had the same bug in a worse form -- it divided the aggregate's three counters, which exclude padding, by the raw log length, which includes it, so all three read low and summed to 94.7% instead of 100%; Section 4's `0.4% against 90.1%` is `0.4% against 99.6%` and `91.4% to 8.7%` is `97.2% to 9.4%`. `budget_drift.py`, `burst_audit.py`, `concentration.py` and `pathwise_price.py` are patched and regenerated with the same input sets and row counts, though they feed only the retired SaTML sections. `reanalyze_logs.py` and `regime_sweep.py` read `output/h1_outputs`, which has **zero** padding, so every 'known truth from the released logs' is untouched. Anything reading the *generated text* is unaffected: the decoded string never contains the pad tokens. A sampled padding scan over every trajectory directory is the way to check a new run (`output/sweep_chat` 33%, `output/phase5/imit_llama70b` 30%, `output/sweep_plain` 17%, `output/phase2/conc_all` 7%). (ai) **A number is checked against its CSV; the CLAIM ABOUT a set of numbers is checked against nothing, and prose that cites a table is not thereby taken from it.** Three of the third read-through's findings are one shape. The causal-policy appendix printed `+0.0215, +0.0150, +0.0110, +0.0045, +0.0070` and called the series *monotonic*; the last value rises. Each of the five lives in its own `order_averaged_h2h_sparse_*.csv`, so no test had ever read them as a series, and no test read the adjective at all. The same sentence said `0% to 85% of **trajectories** never spend` --- 85 is the scoring log's PROMPT denominator (`1 - 73/500`), while `sparse_causal.csv`'s `spent_nothing_pct` is over trajectories and reads `86.7`: one word, two denominators. And Section 5's Repair 1 cites Table 3 and then gave binding rates that are **not Table 3's** --- the prose carried a 500-prompt re-run (`99.3%`, `0.35%`), the table `renyi_price.csv`'s 150 (`99.6%`, `0.4%`) --- with the extra trap that `99.3` is also the number in Table 3's `alpha=1` risky-unchanged cell, so the prose read like a transposed row. Both measurements were real and neither was wrong; the reader is sent to the table, so the prose must quote the table. **Write the guard against the sentence, not only against the cells:** rebuild the series from its CSVs in order, and assert the shape claim matches whether the numbers have that shape (`tests/test_sparse_causal.py`, `tests/test_main_tables.py`). Same class as (j), one level up. (aj) **Two things nothing catches, and one measurement technique that does.** First: **a `\ref` that resolves to the WRONG content.** Section 2's bigger-anchor sentence cited `app:saturation`, which is the *scorer*-scale paragraph and says nothing about a larger safe model. Tectonic exits 0 and `??` is 0, because the label exists --- only a reader following the click sees it. feat-128 had no appendix treatment at all, which is how it happened: one clause in the body and a scoring log. Where a claim's `\ref` is the reader's next step, assert the target paragraph contains the claim (`tests/test_bigger_anchor.py`). Second: **a conditional guard whose condition is a sentence someone will reword retires itself, silently.** `test_the_abstract_claims_the_judge_free_axis_only_because_it_was_measured` triggered on `"no judge at all"`; the same day's abstract rewrite said `"needs no judge"`, so it returned early and pinned nothing, and a skipped branch is a pass. Revived on a looser trigger it immediately caught its own second bug --- a case-sensitive `"judged"` the capitalised sentence start would have failed. Make withdrawal deliberate: if the abstract drops the claim, the section must have dropped it too. Third, the technique: **to find out what size type a figure prints at, measure the compiled page, never the mediabox.** Mediabox arithmetic said 4.2pt and was wrong (the box carries padding that is not content); glyph bbox HEIGHT is also useless, since it moves with which ascenders and descenders a word happens to have. What works is `pdftotext -bbox` and **advance width per character against the same words in body text on the same page** --- a pure ratio, no assumption about `\textwidth` or the canvas. It gave 5.4pt for the legend, 6.5pt after the fix. And raising a legend's size reproduces caution (ad) exactly: the taller box covered the `n=64` label, the same collision the code had already fixed once by moving the corner. The legend size and the panel's `ylim` move together. (t) **`prompt_text` begins with `Complete the prefix:\n`, and a seed built from it hands a BASE model an instruction.** At `--seed-tokens 20` the header eats six of the twenty and leaves fourteen tokens of the novel. `composition_attack.py` has had `--raw-prompt` for this since feat-018; `selection_extraction.py` defined the header constant and never used it, so the 2026-09-13 natural-memorisation arm measured the 70B at nv-recall **0.0000** where `results/natural_memorisation.csv` has the same checkpoint, novel, temperature and `k=-1` at **0.4137**. Harmless for a LoRA memoriser fine-tuned WITH the header (0.3925 on the same passages); disqualifying for one that memorised the book in pre-training. Protocol C7 says "raw passage seeds with no instruction" for exactly this reason. **The tell was the contradiction, not the zero**: a leakage number that disagrees with `natural_memorisation.csv` on the same model and corpus is a pipeline bug until proven otherwise, and a zero is the easiest kind of bug to mistake for a result. Any new extraction arm against a base model must pass `--raw-prompt`. (ae) **A parameter-count cost model is not a price, and quoting one where a deployer reads a price can be backwards rather than merely imprecise.** `analysis/serving_cost.py` prices selection at `n=64` as `61.3x` the metered decoder from `(P_s + P_r)(L_p + T)` -- parameters times tokens -- and that FLOP proxy reached the **abstract**, Section 5, Appendix I and the closing, each carrying the corollary that `61.3x` is "the price of the reward model, not of the mechanism" and therefore that a smaller scorer cuts it. Measured on one exclusively-held card (`analysis/serving_latency.py`, `results/serving_latency.csv`, registered in `results/onset_prediction_serving_latency.md`), the ratio is **`35.4x`** and **the reward pass is `9.3%` of selection's wall-clock while the `64` draws are `90.7%`** -- the opposite of what the parameter counts imply, because scoring `64` candidates is one batched forward pass while drawing them is `64 x 204` sequential decode steps. With a free scorer selection still costs `32.1x`, so the lever is `n`, not the scorer. **Two design rules the measurement depended on:** both arms must serve the same number of tokens so the ratio of seconds-per-served-token IS the wall-clock ratio (assert it -- `serving_latency.py` does), and the count must be the same field for both arms --- `serving_latency.py` uses `aggregate.generation_length_tokens`, which this caution originally called "the decoded length" and **which is actually the padded length**; see caution (ah), which corrects it and shows why the ratio is unaffected. Interleave `SEL,MET,MET,SEL` and report both repeats: the metered arm's spread was `15.3%` on a 25-second job. Same class as (v) and (aa): the number was correctly computed, and it was not the quantity the sentence around it claimed.

 (ak) **The printed size of figure type is a property of the SAVED FILE, not of `figsize`, and two of this file's own cautions had the arithmetic wrong.** Three corrections from 2026-09-17, found while placing the forest plot that replaced Section 3's breadth paragraph. First: **ICLR's `\textwidth` is `5.984`in**, measured off the compiled page (the text column runs `430.9`pt). Caution (af) sanctions `F = 6.9/5.5` for `selection_frontier` on the assumption that it is `5.5`, so that factor over-compensates by `8.8%`; it is harmless there only because the figure's own saved width then brings the net back to about `1.0`. Second, and the one that actually bit: **`bbox_inches="tight"` expands the canvas to enclose anything drawn OUTSIDE the axes.** The forest plot puts its row labels and its cost column in gutters at `x = -0.02` and `x = 1.145` in axes coordinates, so a `figsize=(6.0, 2.62)` saved as a **`9.09`in-wide** file; placed at `\textwidth` that is a shrink of `0.658`, below the `0.70` floor, and `7`pt labels printed at `5.0`. The repair is to reserve the gutters INSIDE the canvas with `fig.subplots_adjust(left=..., right=...)` so the saved width is the width it prints at and a drawn point is a printed point (shrink `0.928`). **Compute every shrink as `\textwidth * placement_fraction / (saved file width)`, read out of `pdfinfo`** --- never out of the `figsize` argument, which is not what was written to disk. Third: **caution (aj)'s advance-width technique assumes the two fonts match, and here they do not.** Matplotlib draws DejaVu Sans and the body is Times (via `newtxtext`), and DejaVu is the wider face, so comparing advance width per character against the same words in body text **overstates** the figure's point size by that difference --- it read `6.9`pt where the geometry says `6.1`, and read `5.0`pt as if it were fine. Use it only against a figure set in the body font; otherwise measure the saved width. All ten figures were then audited this way and every one clears `0.70`.

 (al) **A number that moves from prose into a figure takes its guard with it, or the guard passes by never running.** Converting Section 3's breadth paragraph into a forest plot on 2026-09-17 silently retired **six** guards at once. Every one of them asserted "the prose quotes this band", the bands had not been deleted --- they had moved into a plot no test could see --- and removing Table 1 the same day retired three more. This is caution (ag) with the deletion performed by a figure instead of a page trim, and caution (aj)'s "a skipped branch is a pass" with the branch skipped by relocation. **The repair is to make the figure's data reachable from a test**: `figures/make_figures_v4.py` splits `selection_forest_rows()` (reads `results/*.csv`, returns the plotted rows) from the drawing code, and `tests/manuscript.py::carries_band` asks whether the paper prints a band ANYWHERE it prints bands --- prose or figure. Nine guards were rewired onto it rather than deleted, each mutation-checked in both directions (dropping a figure row fires two; mislabelling the metered row's spend as a certificate fires one; perturbing the quoted KL fires two). A figure-driven paper needs its figures under test, or every conversion from prose costs a guard.

 (am) **`kl_nats` is a CLOSED FORM, and both of this paper's attempts to label it were wrong.** `analysis/selection_decoding.py:kl_best_of_n` returns `log n - (n-1)/n`, the Beirami et al. best-of-n bound: `1.2044` at `n=8`, `2.4970` at `32`, `3.1745` at `64`. Nothing is measured. The column in the headline table was headed **"budget, nats"** until the third read-through, which flattered the metered baseline `12x`; the repair renamed it **"measured KL, nats"**, which is equally false in the other direction and stood for a day, and the handoff recorded the wrong premise ("a *realised* divergence") as the reason for the repair. An appendix table carried the same error in a worse form: one column headed **"realised KL"** over four metered rows that ARE measured mean spends (`0.0109` to `44.8473`) and three selection rows that are bounds. Only the metered decoder's `171.3` is a realisation, and Section 2 and the composition table both say so correctly --- `"the sharper KL bound of $1.204$ nats"`, `"realised KL, measured mean"` --- so the paper knew the distinction and two tables violated it. **Two rules.** First, a comparison that sets selection's BOUND against the meter's MEASUREMENT is conservative and should say which is which rather than hiding both under one header; the appendix table now states it in the paragraph beneath. Second, a guard on a label must check the label against the CODE THAT PRODUCES the column, not against a plausible story about it: `tests/test_selection_claims.py` now asserts every `kl_nats` cell equals `kl_best_of_n(n)` to `5e-5`, so the next rename has to survive that. Same class as (ae) and (ah), and like (ah) the misnaming had been written into the project's own notes, which is the worst place for it.

 (an) **A guard written as a list of phrasings retires itself on the first reword, and a guard satisfied by a DIFFERENT occurrence of its phrase is not guarding its sentence.** Read-through 5, 2026-09-18, found the fifth and sixth instances of caution (am) --- and both walked past the guard (am) installed, because that guard was five exact strings (`"measured KL, nats"`, `"realised KL &"`, ...). The fifth said **`$x$ the \emph{realised} divergence`** in the caption of the paper's ONLY main-text figure, whose x axis carries selection's closed form beside the meter's genuinely measured `171.3`; the axis LABEL said it too, drawn into the PDF, while the function's own docstring had it right all along. The sixth said **`gives exactly $8$, certified and realised alike`** in Section 2's headline amplification sentence --- `selection_decoding.csv` holds only the closed form and **no `analysis/selection_*.py` computes a realised selection divergence at all** --- the one place the paper does measure an amplification is `analysis/contaminated_anchor.py`, whose `amplification` column is `rate / base_rate` for a named event and IS a realisation (`1.000` to `4.000` over twelve contaminated anchors at `n=64`), and which is empty for a clean anchor precisely because the base rate there is `0`; what the paper actually has is stronger and already proved (the tightness paragraph: `q(y*) = 1-(1-p)^n -> np`, so no constant below `n` works), so the sentence now claims that. **Guard the property, not the spelling:** a realisation word within 160 characters of one of selection's closed-form values must have a bound word with it. Three refinements the real text forced, each a false positive that was legitimate prose: match the **bare** value, since `$\log 8 = 2.08$` puts no `$` around `2.08`; **clip the window at LaTeX row breaks**, because a tabular carries the distinction per ROW and a flat window reads the metered row's "measured mean" beside the selection rows' bounds; and allow **"granted"**, the one honest way a realisation word sits beside such a value (a METERED arm granted `log 8` nats up front and measured spending exactly them). **And scope a guard to the sentence it is about:** the overoptimisation concession check asked whether a phrase appeared anywhere in the file, and the new paragraph added that same phrase, so deleting it from the caption left the check passing on the other occurrence --- caught only by its own mutation test. It now locates the `\caption{...\label{tab:...}}` block and asserts inside it.

 (ao) **An unscoped claim ABOUT a set of numbers, correct in the appendix and universal in the body, is caution (af) with the scope rather than the claim deleted --- and one such pair had the paper contradicting itself 580 lines apart in ONE file.** Appendix I: *"the reward overoptimisation that turns such curves over is not observed anywhere on the grid"*, against that same file's judge-free table caption: *"on TriviaQA its accuracy falls with $n$ (Spearman $-0.607$) and at $n=16$ the interval excludes zero on the wrong side"*. Both sentences were true of their own arm; "anywhere on the grid" was not scoped to the arm it was about. Scoping it to *this* grid and then **naming where overoptimisation does occur** turned a contradiction into a finding --- it is why the judge-free headline is majority vote, which has no scorer to overoptimise against. The same pass found Section 4 claiming *"Over ten safe models the margin `s(x)/c_use` rises monotonically with capability"* where its own cited figure caption says *"...in every family"*: pooled by parameter count the series is `1.94, 3.45, 2.05, 4.14, 2.31, 4.07, 4.19, 2.37, 4.63, 5.02` and zig-zags, because KL3M's largest model sits below Pleias' smallest. **Guard the shape BOTH ways** --- `tests/test_margin_monotonicity.py` asserts within-family monotone AND pooled non-monotone, so if pooling ever does become monotone the test fails and says to revisit the wording rather than quietly permitting the stronger claim; `tests/test_overoptimisation_claim.py` rebuilds both series from their CSVs and checks the adjectives against the shapes (verified to have teeth: the same monotone assertion fails on `kl3m17b`'s `0.017/0.052/0.039`). Caution (ai) one level up: the claim about the numbers is what nothing checks.

 (ap) **The judged level of an arm depends on WHICH OTHER ARMS are in the sweep, and a reproduction gate must be written against generation and reward, never against a judged number.** feat-129 Arm A, 2026-09-18. The gate refused the arm: `selection_scaling_n128.csv` disagreed with the committed `selection_scaling.csv` on every shared arm (judge~B `n=1` level `0.435` -> `0.478`, `n=64` gain `+0.142` -> `+0.076`). **The generations were byte-identical** --- `mean_words` matched at all seven shared arms and **32,000 of 32,000 rewards at ranks 0--63 were bit-identical** --- so nothing about the arm was wrong. `judge_batch` is greedy (`do_sample=False`), so the judge is deterministic *given its input*, and the input changed: `selection_scaling.py` builds `distinct = sorted({(p, picks[(p,n)]) for p in pids for n in grid})` and draws one `rng.random()` **per item in that order** to set presentation order. Eight arms give `2,219` distinct served completions where seven give `1,954`, and `265` insertions into a *sorted* list shift the draw for nearly every later item --- so almost every pair was shown in the opposite order, into a judge caution (m) measured as position-dominated (`261` of `500` win shown second, `24` shown first). **Two rules.** First, **a reproduction gate belongs on the reward cache, not on `gain`**: 32,000 floats compared with `==` is both sounder and far stricter than 28 summary cells at `5e-4`, and the judged version could never have passed --- cautions (e) and (m) said so already and the gate was written anyway, which is why a spec defect must be recorded rather than quietly repaired. Second, **never set a judged number from one sweep against a judged number from another sweep with a different grid**; this is NOT the `~0.04` identical-configuration re-run floor the paper reports (that configuration really was identical, and it is not revised by this), it is a separate and larger effect. The registered band survived untouched because it was specified as a **paired difference within one pass**: both arms share the flip sequence, so the re-roll cannot reach it. **Refined the same night by the post-hoc order-averaged check (`results/n128_order_averaged_note.md`): the effect is GRID-dependence, not irreproducibility.** `rng` is seeded from a fixed `--seed`, so the same command with the same grid is deterministic; what moves a single-order level is *adding an arm*. **Order averaging judges both orders and so draws nothing**, making it a deterministic function of the text under a greedy judge --- and it reproduced the committed pass **exactly**, `+0.1045 [+0.0820, +0.1280]` on both, to four decimals on the point estimate and on both interval ends, where single-order moved `+0.142 -> +0.076` on byte-identical text. That is direct evidence for the construction the paper's headline already uses, and the reason a single-order gain is never compared across sweeps (`tests/test_forest_single_pass.py` fails if the forest plot ever reads the n=128 pass). The order-averaged paired `g(128)-g(64)` is `+0.0140 [-0.0015, +0.0295]` --- the same point estimate to four decimals as the registered single-order read and a `2.06x` tighter interval, still containing zero, so SATURATED BY 64 stands under both constructions. **A paired difference is stable where the effect is LARGE relative to its own interval and not where it is marginal, and feat-131 is the counter-example that proves it.** The audited anchor's seed replication is a beautiful precedent --- a disjoint draw moved its LEVELS (`g(8) +0.054 -> +0.027`, `g(64) +0.142 -> +0.115`) and left its paired `g(64)-g(8)` at `+0.0880` both times, to four decimals on the point estimate and near-identically on the interval. It was tempting to read that as licence for paired differences generally; it is not. TinyComma's difference sits about `2.1` interval half-widths from zero and reproduced exactly. **KL3M-1.7B's `+0.0650 [+0.0270, +0.1030]` sits about `1.7`, and a disjoint draw gave `+0.0040 [-0.0330, +0.0400]` --- a move of `0.061` where the precedent moved `0.0000`.** The construction removes the position lottery, which is what the precedent demonstrates; it does not make a marginal reading safe, and the difference between `2.1` and `1.7` half-widths is the whole margin. **feat-133 then tested that rule OUT OF SAMPLE and it held --- with a refinement it did not make.** Comma-7B, the second of the two anchors the claim rests on, had never been re-drawn; at `2.43` half-widths the rule predicted \textsc{replicates} and the disjoint draw read `+0.0880 [+0.0460, +0.1290]` against `+0.1010 [+0.0590, +0.1420]`, a move of `0.013`. So **both** anchors the breadth-at-`n=64` claim keeps have now survived a fresh draw, and the one it drops is the one that did not. **But the ratios do not order the distances** --- `1.71 -> 0.061`, `2.12 -> 0.000`, `2.43 -> 0.013` --- so the rule predicts the **verdict**, whether a reading survives, and not how far a number will move; the audited anchor's agreement to four decimals is a fortunate draw rather than what a stable paired difference owes, and `0.013` is the honest scale. **Do not pool a reading with its own failed replication**: nothing about pooling was pre-registered, the two arms judge disjoint candidate sets, and averaging them is exactly the post-hoc rescue such a document exists to exclude. Note also what a replication pre-registration must NOT contain: feat-131 deliberately wrote **no bit-identity reproduction gate**, because the arm is an independent draw by construction and such a gate would be incoherent rather than merely wrong --- the checks there are distributional (same 500 prompts, `n=1` empty fraction within `0.03` of the committed `0.002`) and were fixed before the run. **And repair a gate before you look at the result it is gating** --- the corrected gate was mutation-tested in five directions (one reward `+1e-6` fails it; a dropped rank-0 row fails it; changing only ranks `>= 64` correctly does not) *before* it was run on the data, so the fix could not be tuned to the answer.

 (af) **A withdrawn claim has to be chased through every section that made it, and a figure drawn at 6.9in is a `\textwidth` figure.** Two lessons from the second full read-through, 2026-09-17. First: when `feat-122` rebuilt the vetting table into two protocol blocks and withdrew "separation complete over eighteen models", the appendix and the Ethics Statement were corrected and **Section 3 was not** --- it still read "the check separates all eighteen models here ... and every model known to hold the work at least half", which is false at the one protocol (OLMo-2-7B's worst passage is `0.2677`). `tests/test_anchor_vetting.py` had only ever looked at `appendix_selection.tex`. A test written for a repair must scan **every** section file, not the one the arm was about. Second: `frontier_scaling` was `\includegraphics[width=0.62\textwidth]` on a `figsize=(6.9, ...)` canvas, a shrink of `0.494`, so its 7pt legend printed at `3.5`pt; `onset_collapse` at `0.8` was `0.638`. Compute the shrink (`printed width / figsize width`) for every figure and keep it above about `0.7`; the only 6.9in figure that may be narrower is `selection_frontier`, which pre-scales its type by `F = 6.9/5.5` --- **and that sanction was wrong as stated, corrected 2026-09-17**: `F = 6.9/5.5` compensates for printing at `\textwidth` and the manuscript places the figure at `0.70\textwidth`, so it under-compensates by exactly that `0.70` and the legend of the paper's only main-text figure printed at about **5.4pt**. See caution (aj) for how to measure a printed type size, which is not by arithmetic on the mediabox. **And moving a legend trades one collision for another:** relocating Figure 1(b)'s legend out of the `\Lambda^*` curve put its frame through the `n=64` label, and the panel needed `ylim` headroom too. The same pass found three quotation-mark defects in one class --- ASCII `"..."` renders as two *closing* quotes, markdown `` `code` `` as two *opening* ones, and a `.bib` title carried the first into the reference list --- now guarded by `tests/test_contaminated_anchor.py::test_no_ascii_double_quote_reaches_the_manuscript`. **And the 158 underfull hboxes that pass found (58 at badness 10000, about one visibly gappy line per page) were all one cause: a 50-character `\texttt{results/onset\_prediction\_...}` is ONE unbreakable token, so TeX moves it whole to the next line and the line it left behind stretches across the measure. The repair is `\allowbreak` after every `/` and every `\_` inside long `\texttt` arguments --- zero-width, zero-penalty, and it **prints nothing**, so unlike `[htt]{hyphenat}` a reader can never mistake the break for a hyphen that belongs to the filename. 158 -> 13, none at badness 10000, 0 overfull throughout, and 15 paths now break across a line, each after a separator the reader can see. **Two traps:** extending the same rule to hyphens made it WORSE (0 -> 7 badness-10000 lines), because more breakpoints move TeX's choices elsewhere --- the one hyphen break that was needed is done by hand; and the insertion broke `test_abstract_consistency.py`'s `results/([A-Za-z0-9_\\]+)` parser, which then read the filename as `\allowbreak`. Any consumer that greps paths out of the manuscript must strip `\allowbreak` first --- **and the `\texttt` argument must not be broken across SOURCE lines**, which is a second way to defeat the same parser and cost a failing `test_every_results_file_the_paper_names_exists` on 2026-09-21: a path wrapped after `anchor\_\allowbreak` was read as the filename `onset_prediction_anchor_`, which of course does not exist. The `\allowbreak`s are what let the line break in the OUTPUT, so the source line may be as long as it needs to be. Pinned by `tests/test_contaminated_anchor.py::test_long_texttt_paths_carry_breakpoints`, which checks EVERY separator --- its first version only asked whether the argument held one `\allowbreak` anywhere and passed unchanged when one of four was deleted.


 (ag) **A hardcoded label in a generated CSV is not a measurement, and a page-budget trim will silently delete a committed concession.** Two lessons from the second reviewer's pass, 2026-09-17. First: `analysis/compute_matched.py` wrote the literal string `"F4 compute-matched head-to-head (0.94x)"` into `results/compute_matched_bands.csv` while the same run's `results/compute_matched.csv` **computed** `cost_vs_metered = 0.92` for `sel05b_n4`. Promoting that result into Section 2 I quoted the label, so the main text said `0.94x` where the appendix said `0.92x` --- caution (j) in a new dress: the second rounding was not a rounding at all but a string a human typed once and never re-derived. **A number in a results file that was not computed by the script that wrote it is a comment, not data**; make the label read the column (`f"({cost:.2f}x)"`), and pin column, label and manuscript together in one test. Second, and more dangerous: the body was at exactly 9 pages, and making room for genuinely required additions meant trimming --- which quietly removed five **committed** concessions (the 0.5B scorer "never reaches the meter", the `61.3x` ratio, "where that bar sits is open", the 1.5B figure's workload, and the odometer's `192`/`332`/`165.0` triple). Every one was caught by a test written when that arm was scored, and every one was right to fire. **Concessions are the first thing a length edit deletes, because they are the sentences that argue against the paper and so read as cuttable.** When the page budget binds, take the space out of the newest additions, never out of a sentence a pre-registration committed the paper to carry; and note that prose trims mostly reflow (caution (n)) while the guards fire instantly, so the tests are the cheaper check. **The abstract is guarded too, and three more fired there the same day** --- the pair count beside the anchor count, the budget the no-separation claim is made at (matched by *regex*, so a rephrasing breaks it even when the substance survives), and "four anchors in three families" where a trim had left "four anchors". Treat each firing as a fact to be re-expressed, never an obstacle to route around.


 (ah) **`aggregate.generation_length_tokens` is the PADDED length, and caution (ae) said the opposite.** `dap/e1.py:197` sets `gen_len = len(gen_ids)` on the raw batched output, so it includes every pad token; the *true* decode length is `own_len = true_gen_len(gen_ids, stop_ids)` at line 216, which is what `bd_i` is truncated to before the three step counters are computed. So **`steps_active + steps_forced_safe + steps_risky_unchanged` partitions the true length and `generation_length_tokens` does not** --- on `output/phase5/sparse_b4.16_t0` the counters sum to `125` where `generation_length_tokens` reads `203`, and they agreed on only `9%` of trajectories. Found 2026-09-17 when a new arm's "active share" looked implausibly small because it was divided by the padded length. **The `35.4x` serving-latency ratio is UNAFFECTED**: `analysis/serving_latency.py` asserts the two arms' denominators are equal, so the count cancels exactly, and in that run the metered arm hit the `200`-step cap with no early EOS (counter sum `8,000` against `8,179`, `2.2%` slack). What was wrong was the *name*: caution (ae) and the script's docstring both called that field "the decoded length, so it is immune to the padding trap", and it is the padding trap. Both are corrected. Same class as (v), (aa) and (ae): the number was right and the quantity named was not --- and this time the misnaming was written into a caution, which is the worst place for it, because the next arm reads the caution rather than the code. **Any arm that needs a true decoded length must sum the three counters.**

 (ab) **`nvidia-smi` is broken in this account, and the reason is a driver runfile in our own repo.** `LD_LIBRARY_PATH` leads with `NVIDIA-Linux-x86_64-580.173.02/` -- 1.5 GB extracted in the repo root, gitignored, not created by this work -- whose `libnvidia-ml.so.1` shadows the system's and does not match the loaded kernel module (`580.178.04`), so NVML refuses to initialise while **CUDA compute is completely unaffected**: `torch.cuda.is_available()` is True, every measured number is correct, and the only casualty is anything that calls NVML. That is what raised `RuntimeError: NVML_SUCCESS == DriverAPI::get()->nvmlInit_v2_() INTERNAL ASSERT FAILED` inside `generate()` in the POST-TRAINING check of four fine-tunes on 2026-09-16, *after* each had written its merged model, whose non-zero exit then made every queue shell skip the sweep behind it. Two repairs, both committed: the diagnostic can no longer fail a run, and `scripts/gpu_env.sh` strips the shadowing directory -- sourced by every GPU launcher, because glibc reads `LD_LIBRARY_PATH` once at exec and a process cannot fix its own search path from inside. **Until a launcher sources it, `nvidia-smi` returns nothing**, so caution (c)'s orphan check and the Compute rule's "check for other users before taking a card" both need `env -u LD_LIBRARY_PATH nvidia-smi` to work at all. Do not delete the directory (it is not ours). `tests/test_bookmia_onset.py` pins the strip by executing it against a poisoned path, not by matching its spelling. (ac) **`\usepackage{times}` emits NO BOLD ROMAN under tectonic, and nothing reports it.** The ICLR template ships `\usepackage{iclr2027_conference,times}`, but XeTeX finds no bold face for that obsolete package and silently falls back to the regular: on 2026-09-16 `pdffonts iclr_2027.pdf` listed exactly two faces over 58 pages --- `LMRoman10-Regular` and `NimbusSanL-Bold`, the latter only the running header --- so **90 `\paragraph` headings and 54 `\textbf` emphases rendered as ordinary body text**, and the body was set in Latin Modern rather than the Times the template asks for. A reader met "...which is what makes it a security claim  Proposition 1 assumes nothing..." and read it as a missing full stop. Tectonic exits 0, 0 overfull, 0 `??`, no font-shape warning anywhere in the log. Isolated by controlled test: `article` alone and `article`+`iclr2027_conference` both emit `LMRoman10-Bold`; adding `times` **or** `mathptmx` emits no bold roman at all; `newtxtext` emits real Times (TeX Gyre Termes) with bold, italic and bold-italic and leaves math in Computer Modern --- which is exactly what a pdflatex build of the template produces. The manuscript now loads `newtxtext` and **`times` must never be reinstated**. The check is one line, `pdffonts iclr_2027.pdf | grep -ci bold`, which must read **at least 2** and never 1: `NimbusSanL-Bold` (the running header) plus `TeXGyreTermesX-Bold`, the real bold roman whose absence is the defect. It read **3** until 2026-09-19, when the appendix reduction deleted the document's only bold-italic --- `\emph{\textbf{Harry Potter}}` in a table of superseded protocol arms --- so `TeXGyreTermesX-BoldItalic` is no longer embedded. That is a content change, not a font regression; what must never come back is a build with no bold roman. Same class as (y) and (z), and worse than either: the defect is document-wide, and the only witness is the embedded font list. (ad) **A matplotlib legend pinned inside a small axes is a rendering defect waiting to happen, and only a reader of the page sees it.** Three of the ten figures had one on 2026-09-16. Figure 7's twelve-entry legend was taller than the 2.2in panel it sat in, so it covered the `alpha=2` panel, its title and the y-axis label with every curve running through its text; Figure 6's nine dotted `s(x)` rules struck through all nine of its legend labels, and its right panel printed the "certificate vacuous" annotation *underneath* two of its own legend entries; Figure 9's `y=1` label was struck through by one arm, and moving it to the opposite edge traded one crossing line for two, so it needed an opaque `bbox` instead. The repair that works for a legend with more entries than the panel has room for is `fig.legend(..., loc="lower center", bbox_to_anchor=...)` below the panels plus `tight_layout(rect=...)`, never a corner `loc`. And in the same pass: **`label=r"$\Lambda^*_s(u)$, Thm.~1"` renders a literal tilde**, because matplotlib is not LaTeX and `~` outside `$...$` is just a character --- it reached the compiled PDF in the legend of the paper's only main-text figure. Render every figure page to PNG and look at it; `grep` cannot see any of this.

 (aq) **A green suite is evidence about the edits that were guarded, not about the edit you just made --- so mutation-test THE CHANGE, not the repository.** Cutting the abstract from `222` words to `200` on 2026-09-19 left all `680` tests passing, and the passing suite was worth nothing as evidence about the cut: mutation-testing the five claims the new text carries found **three** that could be deleted or inverted with nothing failing. First, **the measured price could be replaced by the FLOP proxy** --- `$35.4\times$` -> `$61.3\times$` --- which is caution (ae)'s exact defect restored to the abstract, and `test_every_number_in_the_abstract_appears_in_the_body` is satisfied by either, because Section 3 legitimately quotes `61.3` for *forward passes*. The error is directional and a deployer reads the abstract's number as a price: the proxy implies a smaller scorer cuts the cost, the measurement says the draws are `90.7%` of the clock. Second, **`a \emph{pathwise} certificate of exactly $\log n$ nats` could become `a bound of about $\log n$ nats`** and pass everything --- understating the order (the contribution is that the *strongest* Renyi order is free) and overstating the slack (`log n` is exact; only the sharper KL form carries `-(n-1)/n`). An adjective is not a literal, so no number guard sees it; caution (ai) one level up. Third, **the judge-free claim could be dropped outright**, because `test_the_abstract_claims_the_judge_free_axis_only_because_it_was_measured` is conditional on `"no judge"` appearing and its *withdrawal* branch asserted the phrase `"Off the judge"` was absent from `experiments.tex` --- the v8 restructure reworded that heading to `two tasks with no judge at all`, so the branch became vacuously true and returned a pass on an abstract with the claim deleted. **That is the third instance of caution (aj), and it happened inside the guard whose own docstring warns about exactly it**, because the repair was itself a phrase trigger. **Two rules.** First, **condition a withdrawal branch on measured data, never on a phrase in another section**: the three new guards in `tests/test_abstract_consistency.py` read `serving_latency.csv`, `selection_verifiable_comma7b.csv` and Proposition 1's own `D_\infty`, so a withdrawal has to survive the CSV rather than a rewording. Second, **the cheapest honest check on any manuscript edit is to break each claim it touches and confirm a named test fails** --- five mutations, five failures, source restored byte-identical, and it cost one command. A suite that was green before the edit and green after it has told you only that the edit changed nothing the old edits guarded. **Run the same sweep over the CONCESSIONS and it is worse, because caution (ag) says a length edit deletes those first:** eight of the paper's conceded weaknesses, broken one at a time, left **six** passing. The compute-matched loss --- `$-0.0395$ $[-0.0720, -0.0065]$`, the single most damaging sentence in the paper --- could be sign-flipped with nothing failing, because `0.0395` appeared in **no test in the repository**: `test_compute_matched.py` pins that arm's COST (`0.92x`) and the worst ratio (`61.3x`), so the sentence could keep its cost and silently invert its result. The TriviaQA turnover could be deleted from the Appendix~I prose because `test_overoptimisation_claim.py` is satisfied by the *other* occurrence in the judge-free table caption --- caution (an) again, in a guard written to fix caution (an). `tests/test_concessions_are_guarded.py` now derives all of them from the data that conceded them (the F4 band row, the TriviaQA `gain_hi95 < 0`, both MT-Bench intervals straddling zero at a half-width of `0.09`, and the sixteen novels counted out of `data/copybench_*.jsonl`), each scoped to its own site. **And the sweep found a stale number nothing else could have:** Limitations quoted the onset interval's mean width as `$0.256$`, which matched the table when it held **five** pairs at 100 passages (`d8f1879`, mean `0.2569`); four pairs were added afterwards, the sentence was never re-derived, and the true mean is `0.2854`. `audit_numbers.py` passed it because the literal `0.256` does occur in a committed CSV --- in an unrelated scorer-cost table --- which is exactly the weakness that audit has always had. **A claim about a set whose membership GROWS goes stale silently with every addition**, so derive every mean over the pairs rather than writing it out, and note that the companion figure in that same sentence, `$0.219$`, has no committed source at all: it predates `onset_table.csv`'s git history, and only its twin `$0.215$` can be checked.
 (ar) **A guard that greps a raw `.tex` is a guard on where the source lines break, and reflowing a paragraph breaks it.** Cutting the appendix on 2026-09-19 rewrapped almost every paragraph, and six guards failed for no reason but the wrap: `test_bigger_anchor` wanted `$+0.0150$ $[-0.0065, +0.0365]$` where the value now ends one source line and its interval starts the next, and `passed narrowly`, `generation-run`, `shared vocabulary` and two more matched nothing across a newline. They failed LOUDLY, which is the good direction --- but the repair is `" ".join(text.split())` in the guard, never reflowing the prose to suit the test, and after relaxing one you must mutation-test it: the first attempt at mutating `+0.0150` used a `sed` pattern that itself assumed a space and so the mutation never landed, and the guard "passed" a test that proved nothing. `tests/manuscript.py` already normalises; new guards should go through it. **And the same edit proved which appendix material is load-bearing: five tables the reduction had turned into prose were restored because their guards pin every cell** (`test_compute_matched` the 0.5B/7B grid and the four-scorer ladder, `test_frontier_pair` and `test_frontier_third` the two extra-pair head-to-heads, `test_selection_verifiable` the TriviaQA certificate rows). A table whose cells are guarded is evidence, not layout; the appendix's real floor is set by those, not by prose. **Measured floor, 2026-09-19: the appendix is 119,826 characters of text over 30 pages, and every page is full** --- the low character counts on table pages are tables, not whitespace, and relaxing `\topfraction`, `\textfraction` and `\floatpagefraction` to their usual loosened values moved the page count by **zero**, so it was reverted. Going below about 24 pages means deleting measured arms, not compressing prose. **Done on the user's instruction the same day, and the floor moved to 23:** appendix_seed was retired whole (kept as `sections/appendix_seed_v8_2026-09-19.tex`; the seed and anchor-warp interventions, `S_match/S_20 = 0.392` and the 61%/39% split left the paper), the epoch/seed ladders, the scorer-scale grids, the vetting table, the two extra-pair head-to-heads, the TriviaQA certificate table, `tab:repairs` and five figures went with them, and the two-judge and two-pipeline concessions were compressed rather than dropped. **33 -> 23 appendix pages, 46 -> 35 total.** The rule that held throughout: delete the APPARATUS, keep every concession, every guarded number and every claim the main text points at -- which is why the six cuts returned five pages rather than the eleven their extent suggested. What is left is results --- and the last three pages were bought WITHOUT deleting any of them. **The appendix body is set `\small` from `\appendix` onward** (10pt -> 9pt; floats are already `\footnotesize`, an absolute size, so nothing in a table or caption changes), which took 23 pages to **20** on its own. The 9-page body is untouched and is the only thing the call limits. **Reach for the typographic lever before the scientific one**: the alternative on the table was deleting the contamination arm (Proposition 4's only real test), the climb-to-`n=64` arm (the concession that only two anchors climb reproducibly) or the majority-vote cost table (the paper's own headline), and one step of font size was worth all three. **Final: 46 -> 32 pages, appendix 33 -> 20, main text 9 of 9, 666 tests.**

 (as) **A reproducibility gate must be calibrated against the pipeline's OWN measured noise floor, never against an assumed one --- and at bf16 that floor is enormous.** feat-136 gated a second host by re-scoring the committed arm's own `32,000` candidates there, input text held identical, and required `99.9%` of rewards within `1e-2` and the served argmax to agree on `99%` of cells. It read `17.6%` and `95.7%` and FAILED. The reward model was **byte-identical** on both hosts (same revision, same md5 on the first shard) and the input was the committed arm's own generations, so neither checkpoint nor corpus differed. **The premise was ours, in writing, in the pre-registration: "a different bf16 reduction order moves it by ~1e-3."** That is an fp32 figure. bf16 carries an 8-bit mantissa, about `0.4%` relative, and the reward is a DIFFERENCE of two logits after a softmax over `152,064` tokens through `28` layers. The control that settled it removed hardware from the question entirely --- same host, same weights, same texts, `--batch-size 8` against `16`, which changes only the left padding and hence the reduction order --- and it **also failed the gate**, at `60.0%` of rewards within `1e-2`, mean `|diff|` `0.092` and a max of `3.00`, LARGER than the cross-host max of `1.75`. **The registered threshold was unsatisfiable by two runs of this pipeline on one machine, and a gate nothing can pass gates nothing** --- caution (p) in mirror image, where a gate failed every anchor including the one it was validating. Per caution (w) the numbers are INVALID rather than FAILED, so the arms are not retired by a defect in our own specification. **What may be salvaged and what may not.** The three numeric thresholds are withdrawn, and they stay in the source and in the CSV marked `WITHDRAWN`, because a threshold that vanishes cannot be audited. What survives is the defect SCALE the same paragraph registered before any data existed --- it says the gate exists because a wrong template or a padding-side flip "moves a reward by **whole nats**" --- read literally as `1.0`, which cross-host `0.185` and within-host `0.092` both clear and a wrong chat template does not. **That threshold is defensible only because it comes from words fixed in advance rather than from the number it has to judge**, and `tests/test_host_transfer_gate.py` asserts it stays at least `5x` above the measured value so that moving it toward the data fails by name. **Two rules.** First, **the repair must happen before the data it gates exists, and here it provably did**: all three arms were still generating (`6200`, `3800`, `3800` of `12,800`) and neither `selection_scaling_*hb64.csv` nor `breadth_ladders_scoring.csv` existed --- but the cross-host FAIL *was* read before the threshold was questioned, which is caution (ap)'s rule broken, and the binding consequence is that the repaired bound had to come from an independent within-host control and not from the cross-host number. Second, **say that a repair is a weakening when it is one**: the gate now excludes gross pipeline defects and nothing finer, and it cannot certify that two hosts compute the same reward, because at bf16 nothing can. The instrument question is answered instead by the host-transfer arms' own registered prediction, `|D_hostB - D_local| <= 0.0610`, fixed in advance from feat-131's measurement. **And the control is a RESULT, not only a diagnostic:** the pointwise reward is reproducible to about `0.09` nats within one host, `40%` of rewards move by more than `0.01` on a batch-size change alone, and **the completion best-of-`n` actually SERVES changes on `3.5%` of (prompt, `n`) cells within one host and `5.1%` across hosts** --- a deployer cannot reproduce which candidate was served. (Those rates are over `n >= 2`. The grid-wide figures are `3.0%` and `4.3%`, and they are DILUTED: `n=1` is `14%` of the cells and agrees by construction, so an aggregate over the whole grid averages in a stratum that cannot disagree --- caution (v)'s stratified-rate defect in miniature, and it was in the first version of this caution.) **Both are LOWER bounds**: rewards are stored to 5 decimals and `30` of `500` prompts have exactly-tied top-two values that break by index identically in both caches, so rounding can only make the argmax agree more. **And fp32 does not fix it, which is the counterintuitive part:** fp32 makes the REWARDS essentially exact (`32,000/32,000` within `1e-2`, mean `6e-5`, ~`1500x` better) and the argmax perfectly reproducible at `n <= 8`, but above `n = 16` the served completion still moves on `4-7%` of prompts --- at a cost of `0.00001` nats against bf16's `0.011`. In bf16 rounding overturns a real preference; in fp32 only ties remain, because at `n >= 16` the top candidates sit within `1e-4` nats of each other, which is the SCORER saturating rather than the arithmetic failing. **A repaired threshold is still an assertion about defect magnitude until it is measured:** `analysis/gate_power_probe.py` put four deliberate defects through the committed reward path and read `16.56` (padding side flipped), `9.40` (truncation), `6.30` (prompt/completion transposed) and `5.11` (chat template dropped) against a noise floor of `0.09-0.19` --- a factor of `28` between noise and defect, with the `1.0` threshold between them. Its own first run scored a zero that never fired (no item was long enough to truncate at `512`), which is caution (t) inside the probe built to test a caution. **The certificate is untouched by all of it:** Proposition 1 bounds `q(y) <= n p_s(y)` for ANY score and ANY tie rule, so an argmax that moves under rounding cannot loosen it. The guarantee is numerically robust exactly where the realisation is not, which is a measured reason that score-agnosticism is worth having rather than a technicality. It had no committed bands, so it is a `results/*_note.md`, never an `onset_prediction_*.md`.

 (at) **Two arms compared across hosts must have come from the same PIPELINE, and the runs record
 enough to check it mechanically.** `feat-136`'s first host-transfer arm finished and G0b failed --
 `76.5` words at `n=1` against a registered local `72.2`, `+6.0%` on a `5%` tolerance. **The obvious
 explanation was measured and refuted**, which is the only reason the real one was found: an empty
 completion is `EOS` at step 0 and carries zero words, so caution (v)'s mechanism predicts the empty
 fraction fell; it **rose**, `0.018 -> 0.052`, which pushes a mean length DOWN, while the mean went
 up and the non-empty median moved `57 -> 74`. Within one host a batch-size change -- which alters
 the reduction order and the padding pattern -- moves that quantity `0.4%`. The cause was then read
 straight off the trajectories, which each record `target_model` and `anchor_model`:
 `scripts/run_breadth64.sh` passes `--safe-model-path "$MODEL" --risky-model-path "$MODEL"` and so
 **self-pairs by design** (every local breadth arm reads `target == anchor`), while
 `output/phase5/sel_anchor64`, the directory the `72.2` was taken from, pairs the TinyComma anchor
 with `meta-llama/Llama-3.1-8B-Instruct` and is not a breadth arm at all. **The comparison changed
 the host AND the pipeline**, which is feat-132's shape exactly, so the arm is INVALID rather than
 failed (caution (w)) and the question is not retired. **The defect was confined to one arm and that
 was checked rather than hoped**: the gate applies only to `role=host` arms, and the other host arm's
 counterpart IS a `run_breadth64.sh` arm whose rank-0 mean measures `99.18` against its registered
 `99.2`. **Three rules.** First, a host- or seed-transfer arm must name the **directory** it is the
 counterpart of and let the scorer derive the reference with its own code, never a number typed into
 the pre-registration -- caution (v) said "measure the reference on the arm being replicated" and a
 typed constant is precisely how that instruction gets lost. Second, **assert the pipelines match**:
 reading `target_model`/`anchor_model` off both runs turns an invisible confound into a gate, and it
 is two lines. Third, **the failure message must match the failure** -- a structural failure (no
 counterpart, or one from another pipeline) has a KNOWN cause and must not be reported under the
 `CAUSE UNDETERMINED` wording reserved for a genuine length disagreement, because that tells a
 reader the lengths differ for reasons nobody can separate when in fact **no comparison was made**.
 Same class as (ae), (ah) and (am): the number was computed correctly and was not the quantity the
 sentence around it claimed.

 (au) **A parser must be written against GENERATED OUTPUT, not against output you imagined --- and
 reading a generation's SHAPE is not peeking.** Three arms have now died of the same defect. MMLU at
 the audited anchor twice (a base model echoes the prompt's option list before answering, so the
 answer is not on the first line; `301` of `500` scored wrong and a four-way choice landed *below*
 its `0.25` floor), and LAMBADA once (both models echo the prompt's tail before continuing, so the
 target word is in the completion but is not its first word; the risky model read `0.102` against a
 published `~0.70`). The third time the pre-registration **claimed** the parser had been validated
 on output shapes, and `tests/test_lambada_extraction.py` really did pin it --- to a bare
 continuation, a run-on, quoting, punctuation and an empty string. **Every one of those shapes was
 hypothesised.** The shape that occurs, and that had already killed two arms, is the echo, and it
 was not tested because no generation had been looked at. **The fix is cheap and must be done before
 the pre-registration is written: generate a handful of completions and read them.** That reveals
 FORMAT and not accuracy --- nothing is scored, no band is seen --- so it is not peeking, and
 refusing to do it is not rigour.

 (ay) **A ratio of two totals is not a price, because the model loader is in the denominator and
 it is bigger there --- and the explanation we gave for that ratio was measurable and wrong.**
 `results/serving_latency.csv` prices selection at `n=64` as `35.4x` the metered decoder, against
 the FLOP proxy's `61.3x`, and the scoring log explains the gap by saying that ``64 completions
 generated in one batch do not cost 64 sequential ones''. feat-162 measured both halves on an idle
 second host and **both were wrong**. First, `draws(n) = 10.31 + 6.99n` seconds at `R^2 = 0.99990`:
 exactly linear, because `dap/e1.py:_run_seed_group` batches across **prompts** within one seed and
 realises `--trajectories-per-prompt n` as `n` separate seed groups, so there is no amortisation
 across the candidate dimension at all. Second, the real gap is startup: loading checkpoints is
 `10.97` of the metered path's `17.94` seconds --- **`61%`** --- against `10.31` of selection's
 `458.0`, so dividing one total by the other divides by a number that is mostly a load a served
 request never pays. Per request the ratio is `67.2x`, which is `1.10x` **above** the proxy rather
 than `1.73x` below it. **Time the load apart from the work**, by fitting `a + bn` over a grid of
 `n` and measuring the other arm at one *and two* completions so its own slope is measured rather
 than assumed; a server loads once and serves for hours, so `b` is the number a deployer pays.
 **Two consequences that reach further than the arithmetic.** A compute-matched comparison selected
 on the wrong axis matches the wrong cell: the paper called `n=4` with a `0.5`B scorer
 compute-matched at `0.92x` because the FLOP model divides the price by `4.16` when the scorer
 shrinks, and on the clock that swap is worth `2.4%` of the cell, which really costs `4.11x` --- a
 mispricing of `4.47x`, and the genuinely matched cell is `n=1`, where selection has nothing to
 select from. And **one draw from the anchor costs what one metered decode costs**, `6.99`s against
 `6.97`s --- which we first explained as neither path being weight-bound, **and that was wrong**.
 `a_patch/factory.py` forwards BOTH models at EVERY step whatever `k_radius` is, and the
 `k_radius == 0.0` branch consumes the risky logits only to build a zero budget, so the draw path
 runs the `8.03`B model and discards it. The two agree because **they are doing the same work**.
 feat-163 then measured the consequence without knowing it: `c_a/c_m` is flat to `1%` over a `25x`
 span of batch width, which is what identical work looks like and which no weight-bound story
 predicts. **Before timing two code paths against each other, read what each one runs** --- it was
 two lines of `grep` away, and instead an explanation was fitted to the number and written into
 the manuscript, this file and a scoring log within the hour. feat-164 then measured the three-way
 split at width `200`: the harness path `12.838`s, the same loop self-paired `8.515`s, a plain
 `generate()` with the anchor alone `4.463`s, the meter `13.074`s. Since `B ~ 2C` the loop adds
 nothing and every gap is a model forward, so **a deployment pays `0.35` of every selection
 serving number this project has published** and `n=64` costs `21.8x` a metered decode rather than
 `67.2x`. **An audit harness is the wrong thing to time**: forwarding both models is exactly right
 for comparing a budgeted decoder against its own anchor step by step, and it prices a deployment
 that does not exist. **Also: a timing arm needs an
 idle BOX, not just an exclusively-held card.** The local arm reported a `15.3%` repeat spread on
 its metered cell while two other cards in that machine held `25` and `68` GB of someone else's
 job; on a box with nothing else running the same cell repeats to `0.6%`.

 (az) **An appendix cut keeps the main text's `\ref`, and a comparative word is a claim about an interval.** Auditing referee reports 2--4 on 2026-09-23 found two main-text claims pointing at appendices that no longer held them: the introduction's AUC `0.526` against length's `0.537` (sent to Appendix A, never there) and the failed scorer's `-0.006` / `+0.488` (sent to Appendix H, whose paragraph the 2026-09-19 reduction had cut --- a referee still quoted it from the older build). Nothing fails: the label exists, so tectonic exits 0 and `??` is 0. **After any appendix cut, grep every number the main text attributes to an appendix and assert the target paragraph holds it** (`tests/test_review_r234.py` does this for both). And when the numbers were recomputed with intervals, the comparative in "worse than the answer's length" did not survive: the paired difference is `+0.011 [-0.023, +0.046]`. **"Worse", "better" and "beats" are claims about an interval**; guard the word against the CSV, as `test_the_likelihood_is_called_worse_than_length_only_if_the_interval_says_so` does. The same audit hit three old cautions again, each in a new place: caution (j)'s double rounding (a CSV at 4 decimals turned `-0.023493` into a printed `-0.024`, so the CSV is now at 6); caution (ax)'s overwrite (`scripts/run_cpfuse.sh --out results` replaced the phase-3 `cpfuse_audit.csv` while its scoring log said "untouched", so `analysis/cpfuse_audit.py` now refuses); and caution (n)'s page-budget trap in a sharper form: **one extra line in a paragraph near a page foot cost three**, because the club and widow penalties moved the break and TeX stretched the page's glue, and one extra abstract line pushed the first contribution bullet whole onto the next page, stranding its run-in heading and costing nine. Measure a layout change by building the previous sources side by side and comparing every section heading's page and y, not by reading the last line of page 9.

 (ax) **A new pass that writes the CANONICAL filenames overwrites the closed arm those filenames
 belong to, and the manuscript keeps quoting the numbers that are no longer there.** feat-161 added
 two rungs to `analysis/scorer_scale.py`'s ladder and ran it with `--out results`, which is what
 every previous pass did --- so it wrote `results/scorer_scale{,_bands,_per_prompt}.csv` over
 feat-117's **closed, scored, four-rung** arm. The appendix quotes six values from that arm
 (`+0.0700`, `+0.0040`, `+0.0095`, `87.3%`, `35.2%`, `0.5429`) and they stopped rounding from their
 own CSV the moment the run finished. **Three guards caught it** --- the measured-parameter-count
 check, the Spearman check and the saturation check all failed with ``expected 4 scorers, got 6'' ---
 which is the system working, but only after the fact and only because those guards pin a COUNT.
 The repair is structural: feat-161's outputs live under `scorer_scale_6rung*.csv`, feat-117's were
 restored from git, and `scorer_scale.py` now **refuses** to write a `scorer_scale.csv` that holds a
 different ladder unless `--allow-overwrite` is passed. **Two judged passes must never share a
 file**, because caution (ap) forbids comparing their levels and one file invites exactly that.
 **And the same arm shows why the parameter-count guard exists**: `14.7701`B and `72.7062`B were
 *typed into the source from knowledge*, not counted, in a list whose own comment says ``COUNTED OFF
 THE LOADED MODEL''. Counted properly --- `metadata.total_size / 2` from the safetensors index,
 a method validated by reproducing the committed `7.6156` for the `7`B rung exactly --- the `14`B is
 `14.7700`, so one of the two was wrong in the last digit. **A number you know is not a number you
 measured**, and the guard that would have caught it was the one already failing for the other
 reason. **Two tells that a parser is the problem rather than the model:**
 a forced-choice task reading *below* its own chance floor, and any arm where a model whose
 published score on that benchmark is well known reads far under it --- which is why every such arm
 must carry an instrument gate on the UNCONSTRAINED risky model, read before the entry gate on the
 anchor. LAMBADA's caught it; MMLU's first arm had no such gate and the defect reached a scoring
 log. And **a gate on an accuracy must have a DIRECTION**: MMLU's first gate read "the interval
 excludes `0.25`" and the anchor excluded it *downward*, so read literally it passed a model
 performing at a third of chance.

 (av) **When a pre-registration forbids quoting a LEVEL, the guard has only the VERDICT left --- and
 a verdict is a string that can go stale over a number that moved.** feat-157 excluded, in advance,
 ``reporting a `14`B level beside a `7`B one'', which is right (caution (ap): never set a judged or
 scored level from one sweep against one from another). The consequence nobody anticipated is that
 the appendix paragraph then quotes only verdicts and interval half-widths, so the guards written
 against it read `verdict` and `half_widths` and nothing else. **Sign-flipping the headline gain in
 the CSV fired no test.** A future re-run that wrote a positive gain under a stale `TURNS OVER`
 label would have been invisible, which is caution (ag)'s hardcoded-label defect arriving through a
 column the manuscript deliberately does not print. The repair is two lines: assert the verdict and
 the sign agree (`gain < 0 < -hi95` for TURNS OVER), so the label cannot outlive its number. Found
 only because the change was mutation-tested (caution (aq)) rather than the repository --- and in
 the same sweep **two mutation patterns did not land**, one assuming a line break the source does
 not have and one whose string occurs three times in the CSV, both caught by an `assert
 count == 1` in the mutation harness rather than by a false pass. Put that assert in every mutation
 harness; caution (ar)'s ``a guard that passed a test that proved nothing'' is otherwise the default
 outcome.

 (aw) **A corpus dependency you cannot see is the one that stops a launch, and this project has
 three kinds.** feat-158/159 needed four attempts to get eight cards busy, and every failure was a
 dependency that no test, no sync check and no dry run could have surfaced. **First: a symlink
 directory does not survive `rsync` to another host.** `data/bench/<corpus>/` is a directory of
 symlinks to the committed prompt sets --- the pattern that lets a new corpus take the same
 `h1.py --data-dir` code path as every run on record --- and `build_bench_corpora.py` writes them
 with ABSOLUTE targets. Copied to host B they point at `/mnt/md0/.../DA5001_Project/data/...`, which
 does not exist there, so **six of twelve corpora were silently dangling** and `h1.py` died on
 `Missing required file`. `tests/test_bench_corpora.py` asserts every entry IS a symlink and cannot
 see that it RESOLVES. Repaired by `scripts/fix_bench_symlinks.sh`, which re-anchors any dangling
 link onto the repo it is actually in, and wired into `sync_status.sh push` so it runs on every
 sync rather than being remembered. **Second: a corpus that comes from the `datasets` library is
 not in `data/` at all.** `--task triviaqa` calls `load_dataset("mandarjoshi/trivia_qa")`, which
 under `HF_HUB_OFFLINE=1` fails with `OfflineModeIsEnabled` on a host that has never downloaded it
 --- while the GSM8K arm beside it ran fine, because another user's cache on that box happened to
 hold `openai___gsm8k`. **Depending on a stranger's cache is not a dependency you have**, so both
 datasets now live under `hf_datasets/` in the repo with `HF_DATASETS_CACHE` pointed at them, which
 also keeps the host's work inside the `v` folder it is scoped to. **Third, and the one I got
 half-right: verifying the WEIGHTS of a model is not verifying the MODEL.** Host B's cache held
 only the `Meta-` prefixed Llama-3.1-8B-Instruct while the committed arm records the unprefixed id,
 so the id was resolved through a symlink --- justified by md5-matching all four `safetensors`
 shards. That check is necessary and not sufficient: a decoder's prompt boundary is set by the
 TOKENIZER, and had `tokenizer.json` differed the run would have been a different pipeline under an
 identical recorded name, which G2 compares and would have passed. `tokenizer.json`,
 `tokenizer_config.json`, `special_tokens_map.json` and `config.json` were checked afterwards and
 all four match --- but afterwards is the wrong time. **When substituting one model id for another,
 hash the tokenizer and the config, not only the weights.** **And the launch discipline that
 contained all of this: smoke one card before committing eight.** The first smoke cost four
 minutes and caught the dangling corpora; a second, on `500` prompts at `--trajectories-per-prompt
 1`, both confirmed the committed protocol reproduces on the new host (`0.1580` against `0.1660`,
 inside its own interval, so the registered gate is SATISFIABLE --- caution (as)'s rule that a gate
 nothing can pass gates nothing) and surfaced a shape statistic that had never been baselined,
 `57/500` host-B generations echoing the question tail against `0/500` locally. That was measured
 rather than waved past, because it is caution (au)'s exact shape: an echo-aware parser recovers
 **exactly zero** additional correct answers (`delta +0.0000`), so the committed extractor leaves
 nothing on the table and no parser change was made.

 (ba) **A tokenizer configured for one job silently breaks the next job that reuses it.** `analysis/selection_extraction.py`'s `load_tok()` sets `padding_side="left"` because batched generation needs it, and `score()` then reused the same tokenizer to rank candidates by the memoriser's likelihood, indexing `[plen, mask.sum())` as if the batch were right-padded. For every candidate shorter than the longest in its batch the window lands on the prompt's tail and the padding --- a one-token continuation was scored as `<|end_of_text|>` --- so the adversarial selector ranked partly on padding from the day the script was written (`335f3ea`, 2026-09-11) until 2026-09-23, when writing feat-179's registration led to reading the function. Nothing failed: the scores were finite, the picks plausible, every gate green. **What limited the damage is worth copying**: the headline zero never depended on the selector, because every clean-anchor pool's best draw (`anchor_max_recall`) was already `0.0000`, and the per-passage CSV stored it. A claim about what a selector *serves* is only as good as the selector; a claim about what the *pool contains* needs no selector at all, so store the oracle beside the pick (`oracle_recall_n{n}`, `oracle_rouge_n{n}` now do) and state selector-free claims on it. **Two rules.** Index a padded batch from each row's first unmasked position (`mask[j].nonzero()[0]`), which is right for either side. And test a scoring function the only way that catches this: score each item alone and inside a padded batch and assert they agree (`tests/test_selection_extraction_scoring.py`, a fake model and tokenizer, no download). `analysis/selection_decoding.py` has the same indexing and is correct only because its tokenizer pads right; do not change that tokenizer without changing the indexing.

 (bb) **`ssh host 'X && setsid nohup Y > log 2>&1 < /dev/null & echo ok'` hangs until `Y` finishes, because the `&` binds to the whole `X && Y` list.** The list runs in a background SUBSHELL whose stdout is still the ssh channel --- only `Y`'s own descriptors were redirected --- so ssh waits for the subshell, and a local `timeout` then kills the wrapper (exit `143`) while the remote job carries on. It happened twice on 2026-09-23, launching feat-179's multilingual waiter and feat-178's re-judge; both jobs started correctly and only the wrapper hung, but a wrapper that dies looks exactly like a launch that failed. **Launch as `X || exit 1; setsid nohup Y > log 2>&1 < /dev/null & echo launched`**, or redirect the whole group, `{ X && Y; } > log 2>&1 < /dev/null &`, and confirm the launch with a SEPARATE ssh that reads the process table and the job's log.

 (bc) **A left-padded batch row sliced at its own token count keeps the prompt's tail.** `dap/e1.py` and `dap/e2/evaluator.py` cut each row's `generation` at `prompt_lens[i]`, so a row with `p` pad tokens recorded its last `p` prompt tokens as the start of its generation --- on `73.6%` of the headline's prompts for the opponent, on every creative prompt of the selection pool. Nothing SERVED changed; the JUDGE, which reads the recorded text, did, and the headline difference moved `+0.0645 -> +0.0505` when re-judged on the recovered text (feat-184 A1). Fixed at both call sites (`tests/test_left_pad_slicing.py`); existing text is recovered exactly as `full_text` minus the prompt by `dap.shared.served_generation` (`--deecho`). Any judged number from a run before 2026-09-24 was judged on echo-carrying text unless its note says otherwise.

 (bd) **Satisfying a guard can re-commit the defect a referee just removed, and a guard's vocabulary can drift from the script it checks.** Two from the fifth review round, 2026-09-24. First: a rewrite deleted the two Llama-3.2 "further pairs" from Section 4 because a referee showed readers take them as copyright evidence when neither anchor is a safe model; three guards then failed, and restoring the sentence to satisfy them put the defect straight back, with a parenthetical that made it worse. **When a guard fires on a sentence a review asked to remove, re-derive the guard against the review's point** (here: the numbers must sit beside their "not a safe model" caveat, and Section 4 may quote them only with it) --- caution (ag) protects concessions, not placements. Second: `tests/test_h2h_verdicts.py` expected D5's below-zero label to read `CHALLENGER WINS`, a string no script writes (`order_averaged_h2h.py` and feat-185's registration say `INCUMBENT LOSES`); it passed for two days because no committed D5 row had yet fallen below zero. **Mutate every branch of a verdict guard with a synthetic row before trusting it.** And the mutation itself: `sed 's/,LABEL$/.../'` silently misses in a CSV written by Python's `csv` module, whose lines end `\r\n`, so the guard "passed" a mutation that never landed --- mutate in Python with an `assert text.count(old) == 1`.

## Startup Workflow

Before writing code:

1. `pwd` must be this repo root.
2. Read this file completely, then `.claude-private/plans/radiant-stargazing-newell.md` (plan v5, the current spec) and, before touching Related Work, `~/sub/satml/LITERATURE_REVIEW.md`.
3. Run `./init.sh`. If it fails, repair that first before adding scope.
4. Read `feature_list.json`, `progress.md` and `session-handoff.md`; pick the lowest-numbered feature whose dependencies are `done` and whose status is `not-started` or `in-progress`. As of 2026-09-11 **every feature through feat-087 is `done`** and the manuscript is verified end to end; the remaining work is a full read-through and whatever the plan opens next. feat-010/011 remain optional and unstarted, feat-012 is superseded by feat-024, **feat-060 was wrongly withdrawn on 2026-09-10 and reinstated the same day — it is `done`, and caution (b) is why**, and **feat-016 is human-only and must never be started**.
5. `git log --oneline -5`.

## Working Rules

- **One feature at a time, with one bounded exception.** Exactly one feature `in-progress` at any moment *for work being edited*. **GPU arms may queue in parallel** when separate cards are free, provided each has its bands committed before it starts and its own `results/onset_prediction_*.md` scoring log — which is what keeps parallel arms from becoming scattered half-finished work. Amended 2026-09-12, when feat-096, feat-097 and feat-098 ran concurrently on GPUs 2, 4 and 0+4; `session-handoff.md` must list every one of them and `tests/test_preregistration_count.py` fails if an unscored pre-registration is not named there.
- **Stay in scope.** Touch only files the feature needs. Log unrelated bugs in `progress.md` under Blockers/Risks; do not fix them.
- **Verification required.** Nothing is done without running its evidence command and pasting the output into `feature_list.json`.
- **Baselines are mandatory.** Every experiment that reports a copying or spend metric at some k also reports k = −1 (risky model only) and k = 0 (safe model only) on the same prompts and seeds.
- **Per-trajectory, not per-mean.** A "budget violation" is only a violation if a single trajectory has Z > max(0, B) + 1e-3 or a step has a_t > k_t + 1e-3. Do not reintroduce the empirical-Bernstein proxy, ρ = U_EBB/B_eff, the surrogate ensemble, or the k-DPP archive. If a mean estimate is genuinely needed, use an anytime-valid confidence sequence (`feat-007`).
- **Known truths about He et al. (arXiv 2602.07120), verified in the PDF text on 2026-09-06:** temperature and repetition penalty are applied to both logit vectors *before* the KL solve (App. B), their book experiments use temperature 0.7 and penalty 1.1 (App. D.1), App. E.1 says that at k = 3.0 "the constraint is rarely binding and p* ≈ p_r", and Table 17 (TinyComma 1.8B + Llama 3.1 70B) gives the unconstrained 70B ROUGE-L ≥ τ on 23.0% of CopyBench prompts with word-LCS 10.7 (anchor alone: 0%, 1.7). `data/copybench_test.jsonl` holds 50 passages of *Harry Potter and the Sorcerer's Stone* (Cooper et al. 2025: 96.3% extraction coverage for Llama 3.1 70B) and `data/copybench_attack_train.jsonl` holds 8 of *1984*; the LoRA memoriser never saw the test split. Our decoder applies temperature and repetition penalty to both logit vectors before the solve (`a_patch/factory.py`, decode loop) and refuses greedy decoding under a budget; until 2026-09-06 `_decode` also refused any warper or processor under a budget, so all released-log and phase-1 attack numbers are at temperature 1 with no penalty. That guard was removed on 2026-09-06 (smoke `output/phase2/warp_smoke`: 8B memoriser, τ=0.7, penalty 1.1, k ∈ {1,3}, 0 violations), so phase-2 runs at He et al.'s book settings (setting B) charge the budget against the warped anchor, which is what App. B specifies.
- **Known truths from the released logs** (do not spend compute re-establishing them; cite `results/` once `feat-002` is done): constraint active in 0.19% of steps at k=3 and 6.3% at k=1; 57% of generations identical between k=3 and k=5; zero invariant violations in 26,999 trajectories (all 18 released files, 5.21M decode steps); 96/999 attack_train trajectories (738/8,999 across all six classes) exceed K in realised log-likelihood ratio at k=1; every N=20 held-out row in Stage 2 contains one duplicated sample (seed collision, `feat-003`); the anchor writes the first floor(δ_init/k) tokens of every answer (δ_init ≈ 6 nats). All reproduced in `results/` by `feat-002`.
- **Known truths from the phase-2 runs** (all in `results/`, with the producing commands in `progress.md`; do not spend GPU-hours re-establishing them): across the 8B and 70B suites, > 100,000 new trajectories and queries contain **zero** per-trajectory violations under either accounting rule. Llama 3.1 70B base at He et al.'s book settings (τ = 0.7, penalty 1.1) reproduces 50-token *Harry Potter* windows with single/oracle recall 0.018/0.015 (k=3), 0.069/0.106 (k=5), 0.131/0.244 (k=10), 0.314/0.591 (k=20, where the single-query column equals the unconstrained 0.314 and the oracle column is within noise of its 0.605). Prefix debt is what suppresses low-k recall: switching it off raises k=5 recall from 0.09 to 0.40 (8B) and 0.07 to 0.22 (70B). Pathwise (Δmax) accounting binds far more often than KL at the same k — active steps 1.94%/0.26% (k=3), 1.12%/0.18% (k=5), 0.52%/0.01% (k=10) — and the utility cost is small: risky NLL per token differs by at most 0.08 nats at those budgets. The Freedman certificate (Prop. 3) gives P(L ≥ K+100) ≈ 0.01 at p90 variance and range caps for every k ≥ 3, against an empirical rate of 0 and no trajectory above K. A per-user odometer at B_user = 400 nats (half the anchor's median surprisal of a passage, 849 nats) cuts off every user and caps reconstruction of 50-token windows at 0.30 oracle / 0.26 chained; the bank cap only binds at k = 10.
- **MULTI-GPU RE-OPENED, no stated expiry (user instruction, 2026-09-16 19:25: "use other free
  gpus").** This supersedes the one-card rule below for as long as it stands, and replaces the
  earlier 8-hour window, which opened 10:36 and lapsed at 18:36 unused. **"Free" means free:**
  at 19:25 only GPU 1 was idle --- GPU 0 held another user's 597 MiB and GPU 4 their 57 GB ---
  so the work went to GPUs 1 and 2 and those two cards were left alone. Check with
  `env -u LD_LIBRARY_PATH nvidia-smi` (caution (ab)) before taking any card, and never GPU 3.
  When splitting work already running on one card, kill only the QUEUE SHELL by PID: its CUDA
  child is reparented to init and keeps going (caution (c)), which is what lets the in-flight
  model finish while the remaining jobs are re-dealt. Write a NEW launcher rather than editing
  the running one.
- **(superseded) MULTI-GPU WINDOW, 10:36--18:36 on 2026-09-16 (user instruction, 2026-09-16 10:36: "for the next 8 hours, use all the gpus").** This SUSPENDS, and does not cancel, the one-card rule below. GPU 3 is still never used (4 GB T400) and
  `CUDA_DEVICE_ORDER=PCI_BUS_ID` is still mandatory. When the window closes, everything
  returns to GPU 2 with no further instruction needed: `run_strength_ladder.sh`,
  `run_copybench_seeds.sh`, `run_phase4_baselines.sh` and `run_renyi_baselines.sh` all take
  `GPU` as an override **defaulting to 2**, so a script run with no `GPU=` obeys the standing
  rule by itself. Check for other users with `env -u LD_LIBRARY_PATH nvidia-smi` (caution (ab))
  before taking a card.
- **ONE GPU AT A TIME (user instruction, 2026-09-15 21:00).** Every process this project starts
  from now on shares a **single** card, and that card is **GPU 2**. No fan-out across cards, no
  "queue shell per card" with two cards live, no second card for a parallel arm --- if two jobs are
  needed they queue on GPU 2 in series. The instruction was given while feat-120's last two sweeps
  were running on GPUs 2 and 4 and explicitly allowed those to finish; they were the last two-card
  work. Note this **supersedes** the parallel-arm allowance in Working Rules ("GPU arms may queue in
  parallel when separate cards are free") --- arms may still queue, but on one card. Caution (x)'s
  repair still applies within that card: one queue shell running its jobs in order, no PID capture.
- **Compute.** Local box: 4×A100 80GB (`nvidia-smi` indices 0,1,2,4; GPU 3 is a 4 GB T400, never use it). D2 was answered on 2026-09-06: the 70B ran on GPUs 1+2 and 8B jobs were allowed on the shared 0 and 4; check `nvidia-smi` for other users' load before taking a card. All phase-2 jobs finished on 2026-09-06 19:16 and the GPUs are released. Llama 3.1 70B base fits in bf16 across GPUs 1+2 (141 GB weights) with TinyComma resident; report only bf16 numbers. The DGX SSH key is not installed for this account (recorded 2026-09-05); `dgx-gpu` is not a fallback unless that changes. **Updated 2026-09-19: the user supplied a DGX host (8 GPUs) and one scoping rule --- all work there is confined to a folder named `v`, nothing outside it.** Two things are NOT recorded here on purpose: the host's address and its password. A password must never reach a committed file or a log (the same rule `HF_TOKEN` lives under), and `sshpass -p` is the wrong mechanism on this box regardless, because argv is world-readable through `/proc` and this machine demonstrably has other active sessions. **Corrected 2026-09-22 on the user's word: those sessions are the user's OWN other project, not a stranger's** --- both hosts are shared between this work and theirs under one account, and the `51` GB job that OOM-killed our `neutral` class twice on 2026-09-19 was theirs. The operational rule is unchanged and if anything firmer: never take a card that is busy and never kill a process you did not start, because the thing you would be killing is the user's. What changes is the diagnosis of anything odd on these boxes --- a vanished virtualenv, a rebuilt environment, a disappearing cache is far more likely to be the sibling project than an outsider, and it is coordinable rather than merely survivable. The `sshpass`/`argv` point stands on its own regardless: a password in argv is readable by anyone on the box. The route to take when access is wanted is to install a public key once and use key auth, which is what the host config the user gave already specifies (`IdentityFile`, `PubkeyAuthentication yes`). **As of 2026-09-19 this session could not reach the host at all**: the permission layer refused a bare TCP reachability probe, reading `~/.ssh`, and even a local `nvidia-smi` in the same turn (classified Sensitive Remote Exec / Data Exfiltration / Third-Party Attack / Exfil Scouting). Four refusals, so the work stayed local; unblocking it needs an explicit Bash permission rule from the user rather than another attempt. **Two things must never move hosts even once it is unblocked:** an arm already generating (the three feat-134 classes were ~6 h in) and the `35.4x` serving-latency measurement, which is wall-clock on one exclusively-held card and would be silently invalidated by different silicon. Set `CUDA_VISIBLE_DEVICES` explicitly for every job, **always together with `CUDA_DEVICE_ORDER=PCI_BUS_ID`**: without it CUDA numbers the A100s 0–3 and the T400 4, so `CUDA_VISIBLE_DEVICES=4` lands on the 4 GB card (this cost one failed run on 2026-09-06); with PCI order the indices match `nvidia-smi`. Large model caches go under `hf_cache/` in this repo (gitignored), never the home filesystem; `/mnt/md0` has 3.5 TB free. **The root filesystem `/` filled to 100% on 2026-09-10** (1.8 TB, 0 available), and because the harness writes every tool's stdout to a file under `/tmp` *before* running the command, that broke **all** Bash calls with `ENOSPC` — including the `df`, `du` and `rm` that would diagnose or fix it. Read, Write and Edit kept working, which is the only way out; the actual `rm` has to be run by the user from outside the agent. Redirect long output to a file on `/mnt/md0` and keep scratch, logs and caches off `/` and off `~` (home is on `/`). **`CLAUDE_CODE_TMPDIR` and `TMPDIR` in `.claude/settings.local.json` do NOT work** — verified 2026-09-10: they arrive empty in child processes and the harness still writes to `/tmp/claude-1001/`, because it resolves its own temp path independently of settings `env`. They are left in the file as inert documentation. `TECTONIC_CACHE_DIR` in the same block **does** propagate, so tectonic's package cache is on md0. To actually move the harness's temp tree, export `CLAUDE_CODE_TMPDIR` in the shell profile before launching `claude`. **What fills `/` is not this project:** `hf_cache/` is 194 GB of real files on `/mnt/md0` (not symlinks into `~`), while `~/.cache/huggingface` holds 405 GB of another project's diffusion models and `~/geometry-projects` another 336 GB. Reclaimed 2026-09-10 with the user's approval: `pip cache purge` (29 GB), `npm cache clean --force` (10 GB), `rm -rf ~/.cache/vscode-cpptools` (9.4 GB) → 62 GB free. `uv cache clean` refused, its lock held by another running process; do not force it while the sibling project's GPU jobs are live --- and note that a shared `uv` cache is exactly the mechanism by which one project's environment work can reach the other's.
- **Secrets.** `HF_TOKEN` lives in `.env` (gitignored) and still returned HTTP 401 on 2026-09-06; run local jobs with `HF_HUB_OFFLINE=1`. `hf_cache/` (132 GB, gitignored) has Llama-3.1-8B-Instruct, Llama-3.1-8B, TinyComma, Qwen2.5-7B-Instruct and, since D1 was answered on 2026-09-06, the 70B base as the ungated mirror `unsloth/Meta-Llama-3.1-70B` (`hf_cache/models--unsloth--Meta-Llama-3.1-70B`, downloaded by `scripts/download_70b.py`). `meta-llama/*` stays gated: ask the user for a valid token before any new download.
- **Do not modify** `~/sub/neurips_2026.tex`, `output.zip`, or the committed prompt sets under `data/` (`copybench_*.jsonl`, `neutral`, `creative`, `factscore`). The one writable path there is `data/gutenberg/` — 50 public-domain texts (44 MB) that `analysis/latent_leakage.py` downloads and caches; it is gitignored and re-fetchable, so it may be deleted. New logs go to `output/` (gitignored); summaries go to `results/` (committed, small).
- **Anonymity.** Nothing you write into `~/sub/satml/` or the artifact may identify the authors. The one sanctioned exception is the third-person citation of the earlier audit, `\cite{vijayavallabh2026audit}` (arXiv 2605.28001, added 2026-09-05 at the user's request): refer to it as "an earlier audit", never "our earlier audit".
- **CFP format rules.** `\documentclass[conference]{IEEEtran}`, 10pt, unmodified geometry; ≤ 12 pages of body text; end matter in this order, none of it counted: `Open Science` (what is released, or why not) → `LLM usage considerations` (exact title; must contain the sentence "LLMs were used for editorial purposes in this manuscript, and all outputs were inspected by the authors to ensure accuracy and originality." plus compute justification) → `Ethical Considerations` (optional) → references. Anonymity: no names or affiliations, own prior work in the third person only (no "our earlier audit"), no hint that artifacts are already public, linked material anonymised. Non-existent references are grounds for desk rejection: verify every entry.
- **Paper integrity.** The manuscript (proofread 2026-09-05, rewritten for phase 2 and re-checked 2026-09-06) reports bank-and-burst as attempted but not evaluated (the fine-tuned memoriser ignores filler instructions) and claims no adaptive prompt search; do not describe feat-010/011/012 as run until their evidence exists. **Provenance:** the 26,999-trajectory logs in `output.zip` are from the earlier arXiv 2605.28001 evaluation (Llama-3.1-8B-Instruct + TinyComma), not from He et al., whose paper evaluates TinyComma with Llama 3.1 70B base and sweeps k from 0.1 to 20; the manuscript must say "released runs/logs", never "the mechanism's authors released" or "the model they evaluated". After any edit under `~/sub/satml/`, recompile and check the PDF for `??` and the log for overfull boxes (see Verification Commands).

## Key Facts

| Item | Value |
|---|---|
| Entry points | `h1.py` → `dap/e1.py` (fixed workload); `h2.py` → `dap/e2/runner.py` (search) |
| Core library | `a_patch/factory.py` (`AnchoredDecodingFactory`; `k_radius=-1.0` = risky only, `0.0` = safe only; `constraint='kl'` or `'pathwise'` selects KL or Δmax accounting, `bank_cap=<nats>` caps the bank via `a_patch/bank.py` `bucket_step`). Temperature and repetition penalty are allowed under a budget since 2026-09-06 and are applied to both logit vectors before the solve; greedy decoding under a budget is still refused |
| Stats | `dap/stats.py`: index-only `build_trajectory_seeds`, copying metrics, `budget_check` (per-trajectory max spend / utilisation / invariant), `anytime_valid_cs` (PrPl-EB). The empirical-Bernstein proxy is gone; E2 `rho` now means max per-trajectory utilisation and `certified` means every trajectory within budget |
| Models | anchor `jacquelinehe/tinycomma-1.8b-llama3-tokenizer`; risky `meta-llama/Llama-3.1-8B-Instruct`, its LoRA-memorised variant (`output/memorizing_llama8b`, `recipes/`), and the 70B base (`unsloth/Meta-Llama-3.1-70B`, bf16, two GPUs); optimizer `Qwen/Qwen2.5-7B-Instruct` |
| Released logs | `output.zip` (2.4 GB; `unzip -o output.zip 'output/h1_outputs/trajectories_k3_attack_train.jsonl'` etc.) |
| Log reanalysis | `analysis/reanalyze_logs.py --logs output --out results` (feat-002; writes `results/regime_table.csv`, `llr_tails.csv`, `prefix_debt_forced_tokens.csv`, `surprisal.csv`, `seed_collisions.csv`, `per_trajectory.csv`) |
| Phase-2 analyses | one script per audit in `analysis/`: `composition_attack.py` (`--constraint pathwise`, `--bank-cap`, `--no-prefix-debt`, `--retries`, `--queries-out`), `natural_memorisation.py` (70B, `--figures`), `odometer.py`, `concentration.py`, `pathwise_price.py`, `extraction_cost.py`, `budget_path.py`, `warped_anchor.py`, `latent_leakage.py`, `check_bank_cap.py`, `recheck_violations.py`. Launchers in `scripts/`: `run_natural_memorisation.sh`, `run_bank_cap.sh`, `run_regime_sweep.sh`, `download_70b.py`, `build_artifact.sh` |
| Phase-2 results | `results/`: `natural_memorisation.csv`, `composition_70b.csv`, `composition_8b_{kl,pathwise}.csv` (+ `_per_passage`), `odometer.csv` (+ `_per_passage`), `bank_cap.csv`, `concentration.csv` (+ `_summary`), `pathwise_price.csv`, `prefix_debt_ablation.csv`, `extraction_cost_{kl,pathwise,pathwise_lo}.csv` (+ `_windows`), `budget_path.csv` (+ `_summary`), `warped_anchor.csv`, `latent_leakage_summary.csv`, `burst_audit.csv` |
| Phase-3 analyses | `analysis/separation.py` (Prop. 5, no GPU), `length_scaling.py` (vacuity vs work length), `utility.py` (C12, judged utility with a null arm; no generation, it scores arms the phase-2 sweeps already produced), `cpfuse_audit.py` (second mechanism), `anchor_control.py` (both anchors on one identical span), `merge_prefix_debt.py` (rebuilds the prefix-debt table), `compute_hours.py` (GPU-hours; the `elapsed:<log>` start rule exists because a merge writes some run directories at the *end*, so `dir_birth` reports 0.00). Launchers `scripts/run_prefix_debt_k20.sh`, `scripts/download_second_anchor.py` |
| Phase-3 results | `results/`: `separation.csv` (+ `_summary`), `length_scaling.csv` (+ `_summary`), `utility.csv` (+ `_summary`), `cpfuse_audit.csv` (+ `_examples`), `anchor_control.csv`, `certificate_caps_comma7b.csv` and `certificate_cap_summary_comma7b.csv`, `compute_hours.csv`. Reproduction commands for all of them are in the `### Phase 3` block of `README_artifact.md` |
| Phase-4 analyses | `analysis/regimes.py` (the three regime boundaries from the safe model alone, nats per **character** so it is tokenizer-invariant; also `event_bound`, the order-invariant vacuity threshold), `anchor_scaling.py` (10 safe models x 3 corpora, + `paired_trend` for the sign test), `budget_drift.py` (CPU only), `opening_effect.py` (`--tag` per anchor). New constraint `a_patch/renyi.py` (`--constraint renyi[:alpha]`, alpha=1 is the KL decoder, alpha->inf is pathwise) |
| Phase-4 results | `results/`: `anchor_scaling.csv` (+ `_summary`, `_paired`), `budget_drift.csv`, `opening_effect{,_tinycomma18b,_pleias3b,_kl3m37b}.csv` (+ `_summary`). Runs under `output/phase4/`; the comma-7b memoriser is `output/phase4/memorizing_comma7b` (nv-recall 0.915 on train, **0.000** on the held-out test split) |
| Phase-5 the granularity gap | `analysis/tokenizer_rates.py --survey` scores 21 ungated, openly licensed causal LMs on the protected passages (**tokenizer files only, no weights**, needs the network) -> `results/tokenizer_survey.csv`. The region is sparse rather than empty — sixteen models at 3.49–4.18 chars/token, four at 1.22–2.36 — and **exactly one** falls in the 2.4–3.4 gap Limitations called unfillable: `cyberagent/open-calm-1b` at 2.71. `scripts/materialise_anchor.py` re-saves an anchor published only as `pytorch_model.bin` (the factory loads with `use_safetensors=True`), verifying every tensor; `analysis/grid_from_sx.py` applies the pre-registered k/s(x) grid rule mechanically. The eighth pair lands at onset/s(x) = **1.027**, between the two clusters, and the context ranking strengthens to **-0.946** at exact p = 0.0013 |
| Phase-5 onset on a second corpus | `--corpus-file` is additive on `analysis/composition_attack.py` and `analysis/onset_theory.py` (there the manifest's third field becomes the **anchor model**, since no `budget_path.csv` exists for a standalone corpus, and `s_s` and `s_r` are then measured on the same raw prefix). `analysis/onset_gutenberg.py` scores it against the bands committed in `results/onset_prediction_gutenberg.md`; outputs `results/onset_gutenberg.csv`, sweeps under `output/phase5/fineg_*`. **The central split reproduces**: KL3M-520M again above 1 with its interval excluding 1, both coarse pairs below, every ratio within 0.05 of its CopyBench twin |
| Selection anchoring, Sections 5-6 (feat-087/088) | `analysis/selection_decoding.py`, `selection_crossjudge.py`, `selection_extraction.py`, `selection_scaling.py`. Draw n completions from the anchor, score them, serve the argmax: `q(y) <= n p_s(y)` for ANY score and ANY tie rule, so `D_inf(q||p_s) <= log n` -- the **pathwise** budget, the strongest order in the family, which a metered decoder buys only by dropping to serving p_r unchanged at 8.7% of steps. Vacuity at `n = e^S(x)`, and `S_total_median = 849` in `odometer.csv` is where the 849 comes from (it was quoted as 850 until 2026-09-11, a second rounding of an integer). feat-088 adds a **pointwise** reward, `log p("Yes") - log p("No")` from Qwen on one fixed template, which never touches the risky model, plus a second scoring judge and the sweep to n=64 |
| Manuscript sections, v6 | `iclr_intro` (the claim + four falsified hypotheses), `frontier` (Props 1-2, Thm 1, the asymmetry), `onset` (0.35 page), `orders` (three repairs + the AUC diagnosis), **`selection`** (Prop 3), **`experiments`**, `related_work_v4`, `iclr_closing` (limitations + conclusion; broader impact is in the uncounted Ethics Statement). Appendices in main-text order: proofs, opening, **onset**, robustness (scaling/collapse/matched), seed, second_anchor, selection, limitations, related |
| Selection anchoring (feat-087, superseded row) | `analysis/selection_decoding.py`, `selection_crossjudge.py`, `selection_extraction.py`. Draw n completions from the anchor, score them, serve the argmax: `q(y) <= n p_s(y)` for ANY score and ANY tie rule, so the certificate is Proposition 1 with **K = log n** and the vacuity threshold sits at `n = e^S(x)`, about e^850. A per-token budget `kT` grows with the work and `log n` does not. **Ranked by the risky model's own likelihood it buys nothing** (0.319 -> 0.313 at n=8, refuting its committed band): within prompt that likelihood predicts the judge at AUC 0.526 [0.504, 0.548], its summed form 0.478 [0.454, 0.500] (chance, not below it), the completion's LENGTH 0.537 [0.514, 0.560] -- **no better than length, not worse**: the difference is +0.011 [-0.023, +0.046] (`analysis/currency_auc.py`, `results/currency_auc.csv`, 2026-09-23). Select with judge A and score with judge B and n=8 gains +0.081 [0.034, 0.130] for 1.204 nats, where the metered decoder's best arm gains +0.072 for 171.3 -- 57.6x the frontier against 7994.6x. Near-verbatim recall is **0.0000 at every n up to 64**. Pre-registration and scoring: `results/onset_prediction_selection.md`; the manuscript section is `sections/appendix_selection.tex` |
| Matched-context intervention | `analysis/context_intervention.py` scores the nine pairs at the benchmark's fixed 20-token seed against a matched 13.6-15.0 **words**, both in one pipeline through `analysis/seed_effect.py --manifest results/matched_context_runs.tsv`. Primary metric is `lcs_word`, because a longer seed shortens the target and an absolute count cannot be inflated by that. Spread 0.289 -> 0.113 (cv 9.6% -> 4.2%), `S_match/S_20 = 0.392` against a committed `<= 0.5`; near-verbatim gives 0.399. Five pairs are unmoved **because they were already at the matched context** -- they are the control, not the effect. Pre-registration and scoring: `results/onset_prediction_matched_context.md` |
| Phase-5 onset, nine pairs | `results/onset_table.csv` is the paper's Section 4 table: nine (anchor, risky) pairs, nine distinct anchors, **six** tokenizers (Pleias, KL3M and open-calm each share one -- verified by hashing `get_vocab()`, not by vocabulary size). Ratios run 0.878--1.166 and fall with the words a 20-token seed buys the adversary, Spearman **-0.958** at exact p = 0.0002. The eighth pair (`open-calm-1b`, the only one of 21 surveyed tokenizers in the 2.4--3.4 characters-per-token gap) and the ninth (`open-calm-3b`, its contingent control: same tokenizer, s(x) +0.5%, a memoriser 5.1x stronger) both land between the clusters, at 1.027 and 0.993. Pre-registration and scoring: `results/onset_prediction_granularity_gap.md`; regenerate the whole chain with `scripts/add_pair.sh` then `analysis/{onset,onset_table,onset_units,collapse_robustness,onset_ladder,onset_burstiness,seed_effect,natural_pair,surprisal_cdf,score_predictions}.py` |
| Phase-5 analyses | the matched-utility line: `analysis/order_price.py` (one published k, four orders), `order_frontier.py` (the 12-point k-grid, `--dtype`, `--split`, `--corpus-file`, `--ordinary-split`, `--seed-tokens`, `--published-k`), `order_law.py` (every grid k treated as the published budget), `order_predictors.py` (six candidates, exact permutation p), `order_crossings.py` (exploratory, gated on each pair's own bf16-vs-fp32 floor), `order_seed.py`, `marginal_price.py`, `proxy_budget.py`, `build_gutenberg_excerpts.py` (600 excerpts of 50 public-domain books -> `data/gutenberg/excerpts.jsonl`, gitignored), `corpus_file.py` (a standalone JSONL reader, deliberately separate from `dap.shared.load_prompt_corpus` so the committed prompt sets stay untouched) |
| Phase-5 results | `results/`: `order_frontier_<tag>_bf16{,_matched}.csv` for **twelve pairs in seven families**, each with its own float32 twin; `order_law{,_summary}.csv`, `order_predictors{,_summary}.csv`, `order_crossings.csv`, `order_seed.csv`, `marginal_price_table.csv`, `proxy_budget{,_summary}.csv`, `gutenberg_excerpts.csv`. **Arms are named off the pair prefix and skipped by name:** `order_seedarm_*` (seed), `order_frontier_gut_*` (second corpus), `order_work_*` (ordinary workload) — one pair re-run is not two pairs, and `order_law.py`, `order_predictors.py` and `order_crossings.py` each carry the `_seed`/`_gut_`/`_work` guard, asserted by `tests/test_order_seed.py`. The pre-registration and scoring log is `results/onset_prediction_orders_matched.md`: every band committed before its run |
| Scale-up corpora (2026-09-12) | The HF token in `.env` works again, so the datasets the scale audit said were missing are on disk under `data/bench/` (gitignored, re-fetchable). `analysis/build_bench_corpora.py` builds each as a **directory of symlinks** to the five committed files plus one real file, because `data/*.jsonl` is read-only and `h1.py` takes `--data-dir` -- so a run on a new corpus is the same code path as every run on record, and `tests/test_bench_corpora.py` asserts every entry is a symlink. `data/bench/bookmia100/` is the 50 BookMIA books marked **seen** (4,935 passages; 31/8/11 books in attack_train/val/test, split BY BOOK so caution (h) holds) and `bookmia100unseen/` the 50 marked **unseen** -- a corpus-level negative control the paper never had. Our protected corpus goes 16 novels -> 50, 758 passages -> 4,935. `data/bench/alpaca/` (805) and `data/bench/mtbench/` (80, eight categories) are the standard instruction benchmarks and go through the **factual** slot, never the neutral one: `dap/shared.py` prepends `Complete the prefix:` to every copyright-domain prompt, which would stop AlpacaEval's number being AlpacaEval's number. |
| Blocklist baseline | `analysis/blocklist.py` (zero GPU) runs the MemFree n-gram rule over generations already on disk, at three corpus sizes and three orders -> `results/blocklist_{data,bookmia100,bookmia_all}.csv`. **It refuted the argument it was built to support:** collateral on ordinary text rises 0.000% (758 passages) -> 0.140% (4,935) and then FLATTENS at 9,870, where the index doubles to 2.24M n-grams and the collateral does not move. A blocklist's collateral is bounded; do not claim it scales with the corpus. What survives is that its guarantee NAMES the works it was given, so it is silent on unlisted works, paraphrase, and an adversary who supplies the prefix. |
| Safe-model set | 10 ungated, permissively licensed models in `hf_cache/` via `scripts/download_safe_models.py`: KL3M 170m/520m/1.7b/3.7b, Pleias 350m/1.2b/3b, Comma 1T/2T (7B), TinyComma 1.8B. `meta-llama/Llama-3.1-8B-Instruct` in the local cache has **no tokenizer**; the `Meta-` prefixed duplicate does and shard 1 is md5-identical, so score with that id. **Setting `HF_HUB_CACHE=$PWD/hf_cache` hides everything that lives in `/home/sports/.cache/huggingface/hub`** — symlink it in, the pattern already used for tinycomma. Done so far for the four `meta-llama/*` models and `Qwen/Qwen2.5-7B-Instruct` (the judge); this has cost two failed runs |
| Artifact | `artifact/` (committed, 579 files + `MANIFEST.sha256`), rebuilt by `scripts/build_artifact.sh artifact` → `artifact.zip` (11 MB, gitignored). The builder excludes `hf_cache/` and, since 2026-09-11, `data/gutenberg/` — 44 MB of public-domain books that an earlier build shipped and committed, against `README_artifact.md`'s own statement that they are re-fetched rather than shipped; they are still in git history, which was not rewritten; `README_artifact.md` is its README and lists every reproduction command |
| Paper, bib, figures | `/mnt/md0/IITM/BackUp/Home/vijayavallabh/sub/satml/satml_2027.tex` + `sections/*.tex` (SaTML version). **`satml_2027_arxiv_v1.tex` is not a variant of this paper**: dated 2026-08-19, self-contained, it is the source of the earlier published audit **arXiv 2605.28001**, cited as `vijayavallabh2026audit` and rendered as reference [6]. Its title must keep matching that bib entry; never retitle or edit it. There is currently no arXiv build of the SaTML paper, `references.bib` (138 entries, all verified; additions are also kept in `bib_additions_2026-09-06.bib` and `bib_additions_2026-09-07.bib`, explained in `LITERATURE_REVIEW.md`, with the 2026-09-06 verification reports in `bibcheck_2026-09-06/`); `figures/` (copied by `figures/make_figures.py --copy-to <that dir>`); appendix proofs in `sections/appendix_theory.tex`; PDF checkpoints `satml_2027_phase1_2026-09-06.pdf` and `satml_2027_phase2_2026-09-06.pdf`; section backups `sections/intro_v1_2026-09-05.tex` and `sections/related_work_v2_2026-09-06.tex`. Compile with `~/.local/bin/tectonic -X compile satml_2027.tex` (no pdflatex on this box). `~` is `/home/sports` here, not the project home: use absolute paths |
| Tests | `tests/` (224 tests: seeds, invariant, chat template, metrics, confidence sequence, E2 stats, plus `test_pathwise.py`, `test_bank_cap.py`, `test_warp.py`, `test_budget_path.py`, `test_composition_helpers.py`, `test_latent_leakage.py` and the phase-3 additions `test_separation.py`, `test_length_scaling.py`, `test_utility.py`, `test_cpfuse.py`, `test_compute_hours.py`, and the phase-5 additions `test_marginal_price.py`, `test_proxy_budget.py`, `test_order_price.py`, `test_order_frontier.py`, `test_order_law.py`, `test_order_predictors.py`, `test_order_crossings.py`, `test_order_seed.py`, `test_corpus_file.py`, `test_order_law_table.py`, `test_onset_gutenberg.py`, `test_onset_burstiness.py`); `./init.sh` runs them |

## Verification Commands

```bash
./init.sh                      # full baseline verification (fails fast)
.venv/bin/python -m pytest -q tests   # 224 tests
.venv/bin/python h1.py --k-values 1.0 --trajectories-per-prompt 2 \
  --cap-neutral 2 --cap-val 2 --cap-test 2 --cap-attack-train 2 --cap-factual 2 --cap-creative 2 \
  --output-dir output/smoke   # GPU smoke test for E1 (one A100; set CUDA_DEVICE_ORDER=PCI_BUS_ID)
.venv/bin/python analysis/recheck_violations.py --queries output/phase2/comp8b_kl/queries.jsonl --constraint kl   # per-query invariant recheck (--constraint is required)
```

```bash
# manuscript: compile, then confirm no unresolved references, no overfull boxes, <= 12 pages
cd /mnt/md0/IITM/BackUp/Home/vijayavallabh/sub/satml && ~/.local/bin/tectonic -X compile satml_2027.tex 2>&1 | grep -ci overfull; \
  pdftotext satml_2027.pdf - | grep -c '??'; /mnt/md0/IITM/BackUp/Home/vijayavallabh/DA5001_Project/.venv/bin/python -c "from pypdf import PdfReader; print(len(PdfReader('satml_2027.pdf').pages))"
```

Per-feature evidence commands are in `feature_list.json`.

## Definition of Done

A feature is done only when ALL of the following are true:

- [ ] The behaviour in its `description` exists and its evidence command ran; the command and its output are pasted into `evidence`.
- [ ] `./init.sh` passes.
- [ ] Any new number that will appear in the paper is written to `results/*.csv` with the producing command in `progress.md`.
- [ ] Repository remains restartable: a fresh session can run `./init.sh` immediately.

## End of Session

Before ending a session:

1. Update `progress.md` (Current State, What's Done, What's Next, Blockers).
2. Update `feature_list.json` status and evidence.
3. Fill `session-handoff.md` (Current Objective, Files Changed, Recommended Next Step).
4. Commit with a descriptive message; leave the tree clean and restartable.

## Escalation

- **GPU job fails or DGX unreachable:** record in `progress.md`, move to a feature that needs no GPU (`feat-002`, `feat-013` prep, `feat-014`).
- **A result contradicts a Known Truth above:** treat it as a bug in the new code until proven otherwise; do not rewrite the paper's claims on one run.
- **Fine-tuning cost or any run > 24 GPU-hours:** stop and ask.
- **Anything that would identify authors, delete `output.zip`, or push to a remote:** ask.
