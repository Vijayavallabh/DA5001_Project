# Guards changed by the v10 restructure (2026-09-24)

The v10 rewrite ("reframe the paper around its contribution ... figure/table-driven ... cut text")
moved or cut text that 231 guards read. Every guard was re-pointed to where its claim now lives, or
its claim was restored to the manuscript; five reconciliation passes (groups A--E) mutation-tested
the re-pointed guards on a scratch copy. No concession, failed prediction or disclosure was retired.
What follows is the complete list of assertions that no longer check what they used to, and why.

| test | what changed | why |
|---|---|---|
| `test_margin_monotonicity::test_the_main_text_carries_the_family_scope` | positive placement check -> negative guard: any body sentence that brings the margin trend back must carry "every family" | v10 moved the margin trend out of the main text into Appendix G, where `test_the_appendix_caption_still_says_the_same_thing` pins both scopes |
| `test_selection_claims::test_the_n_sweep_arms_round_from_selection_scaling_csv` | judge C's `n=64` gain (+0.117) no longer required in Section 4 | corroboration, not a concession; judge C's agreement is carried by Figure 3's head-to-head row, guarded by `test_v10_restructure.py` |
| `test_review_r234::test_the_gain_ratio_is_quoted_with_its_interval_and_only_while_it_excludes_one` | the appendix half (2.61x, its interval, Fieller, undefined fraction) is now conditional on a ratio being quoted | v10 quotes no gain ratio anywhere; the intro half is unchanged |
| `test_manuscript_structure::test_no_ladder_sentence_cites_the_appendix_that_has_no_ladder_in_it` | the target of `app:scaling`/`app:collapse` is read at paragraph granularity; the premise checks the ladder's endpoints instead of the word "ladder" | both labels are paragraph labels inside Appendix G since v10 |
